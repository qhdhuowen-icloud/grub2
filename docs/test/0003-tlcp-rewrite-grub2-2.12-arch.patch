diff --git a/grub-core/kern/efi/tlcp.c b/grub-core/kern/efi/tlcp.c
index 683ed93..ed05b7f 100644
--- a/grub-core/kern/efi/tlcp.c
+++ b/grub-core/kern/efi/tlcp.c
@@ -1,6 +1,19 @@
+/*
+ *  GRUB  --  GRand Unified Bootloader
+ *  iTrustMidware TLCP EFI implementation for grub2-2.12
+ *
+ *  Implements TPM2-based policy state checking and key unsealing.
+ *  All TPM2 command sequences follow the TCG TPM2.0 Library Specification.
+ *
+ *  Policy binding:
+ *    Hardware key (0x81010101): PCRs 0,1,2,3,6,7 (BIOS, device, secure boot config)
+ *    Software key (0x81010100): PCRs 8,9,12     (kernel, initrd, GRUB modules)
+ *    NV index      (0x01800100): policy state metadata
+ */
+
 #include <grub/efi/efi.h>
 #include <grub/efi/tpm.h>
-#include <grub/efi/tlcp.h>
+#include <grub/efi/tpm2tis.h>
 #include <grub/efi/tpm2nvstorage.h>
 #include <grub/efi/tpm2getcapability.h>
 #include <grub/efi/tpm2session.h>
@@ -11,809 +24,658 @@
 #include <grub/tpm20.h>
 #include <grub/tlcp.h>
 #include <grub/file.h>
-
-#define POLICY_STATE_NV                     0x01800100
-#define SOFTWARE_POLICY_KEY                 0x81010100
-#define HARDWARE_POLICY_KEY                 0x81010101
-#define TPMA_PERMANENT_OWNERAUTHSET         0x00000001
-#define TPMA_PERMANENT_ENDORSEMENTAUTHSET   0x00000002
-#define TPMA_PERMANENT_LOCKOUTAUTHSET       0x00000004
-#define TPMA_PERMANENT_RESERVED1            0x000000f8
-#define TPMA_PERMANENT_DISABLECLEAR         0x00000100
-#define TPMA_PERMANENT_INLOCKOUT            0x00000200
-#define TPMA_PERMANENT_TPMGENERATEDEPS      0x00000400
-#define TPMA_PERMANENT_RESERVED2            0xfffff800
-
-#define HARDWAREFILE "/hardware_state.bin"
-#define SOFTWAREFILE "/software_state.bin"
-#define POLICYFILE   "/policy.bin"
-
-#define SET_PCR_SELECT_BIT( pcrSelection, pcr ) \
-                                              (pcrSelection).pcrSelect[( (pcr)/8 )] |= ( 1 << ( (pcr) % 8) );
-
-#define CLEAR_PCR_SELECT_BITS( pcrSelection ) \
-                                              (pcrSelection).pcrSelect[0] = 0; \
-                                              (pcrSelection).pcrSelect[1] = 0; \
-                                              (pcrSelection).pcrSelect[2] = 0;
-
-#define SET_PCR_SELECT_SIZE( pcrSelection, size ) \
-                                              (pcrSelection).sizeofSelect = size;
+#include <grub/mm.h>
+#include <grub/misc.h>
+
+/* TPM persistent handle for software policy key (PCRs 8,9,12) */
+#define SOFTWARE_POLICY_KEY   0x81010100
+/* TPM persistent handle for hardware policy key (PCRs 0,1,2,3,6,7) */
+#define HARDWARE_POLICY_KEY   0x81010101
+/* NV index storing the policy state (1=audit, 2=supervisory) */
+#define POLICY_STATE_NV       0x01800100
+
+#define HARDWAREFILE  "/hardware_state.bin"
+#define SOFTWAREFILE  "/software_state.bin"
+#define POLICYFILE    "/policy.bin"
+
+/* PCR selection helper macros */
+#define SET_PCR_SELECT_BIT(sel, pcr) \
+    ((sel).pcrSelect[((pcr)/8)] |= (1 << ((pcr) % 8)))
+#define CLEAR_PCR_SELECT_BITS(sel) \
+    do { (sel).pcrSelect[0] = 0; (sel).pcrSelect[1] = 0; \
+         (sel).pcrSelect[2] = 0; } while (0)
 
 enum POLICY_STATE_CODE {
-    POLICY_NOT_EXIST,
-    AUDIT_POLICY_EXIST,
-    SUPERVISORY_POLICY_EXIST,
+    POLICY_NOT_EXIST       = 0,
+    AUDIT_POLICY_EXIST     = 1,
+    SUPERVISORY_POLICY_EXIST = 2,
 };
 
-static unsigned int change_endian_dword(unsigned int p)
+static unsigned int
+change_endian_dword (unsigned int p)
 {
-    return (((const unsigned int)(((p) & 0xFF)        << 24)) | \
-            ((const unsigned int)(((p) & 0xFF00)      <<  8)) | \
-            ((const unsigned int)(((p) & 0xFF0000)    >>  8)) | \
-            ((const unsigned int)(((p) & 0xFF000000)  >> 24)) );
+    return (((p & 0x000000FF) << 24) |
+            ((p & 0x0000FF00) <<  8) |
+            ((p & 0x00FF0000) >>  8) |
+            ((p & 0xFF000000) >> 24));
 }
 
-static void unmarshal_uint32(unsigned char *buffer, unsigned int *value)
+static void
+unmarshal_uint32 (unsigned char *buffer, unsigned int *value)
 {
-    *value = change_endian_dword(*(unsigned int *)buffer);
-    return;
+    *value = change_endian_dword (*(unsigned int *)buffer);
 }
 
+/* -------------------------------------------------------------------------
+ * Policy state detection
+ * ---------------------------------------------------------------------- */
+
 /*
- * @return : TSS2_APP_RC_BAD_POLICY_STATE          abnormal policy state
- *           TSS2_APP_RC_POLICY_NOT_EXIST          policy not exist
- *           TSS2_APP_RC_SUPERVISORY_POLICY_EXIST  supervisory policy exist
- *           TSS2_APP_RC_AUDIT_POLICY_EXIST        audit policy exist
- * */
+ * check_tpm_policy_state - query TPM NV and persistent handles to determine
+ * whether a TLCP policy has been deployed.
+ *
+ * Returns:
+ *   TSS2_APP_RC_AUDIT_POLICY_EXIST       — audit policy active
+ *   TSS2_APP_RC_SUPERVISORY_POLICY_EXIST — supervisory policy active
+ *   TSS2_APP_RC_POLICY_NOT_EXIST         — no policy
+ *   TSS2_APP_RC_BAD_POLICY_STATE         — inconsistent state
+ */
 grub_err_t
-check_tpm_policy_state(void)
+check_tpm_policy_state (void)
 {
-    int                     result = 0;
-    grub_err_t              tpm_rc = 0;
-    int                     soft_key_exist = 0; //0 not exist, 1 exist
-    int                     hard_key_exist = 0;
-    int                     nv_exist = 0;
-    TPMI_YES_NO             more;
-    TPMS_CAPABILITY_DATA    capability_data;
-    TPMS_AUTH_COMMAND       session;
-    unsigned int            count = 0;
-    TPM2B_MAX_BUFFER        out;
-    unsigned int            policy_state = 0;
-
-    //0. startup
-    grub_tpm2_startup(TPM_SU_CLEAR);
-
-    //1. check owner state
-    tpm_rc = grub_tpm2_get_capability(TPM_CAP_TPM_PROPERTIES, TPM_PT_PERMANENT, 1, &more, &capability_data);
-    if(tpm_rc)
+    int                  result = TSS2_APP_RC_POLICY_NOT_EXIST;
+    grub_err_t           tpm_rc;
+    int                  soft_key_exist = 0;
+    int                  hard_key_exist = 0;
+    int                  nv_exist = 0;
+    TPMI_YES_NO          more;
+    TPMS_CAPABILITY_DATA capability_data;
+    TPMS_AUTH_COMMAND    session;
+    unsigned int         count;
+    TPM2B_MAX_BUFFER     out;
+    unsigned int         policy_state = 0;
+
+    /* 0. Ensure TPM is started */
+    grub_tpm2_startup (TPM_SU_CLEAR);
+
+    /* 1. Check owner auth is set (indicator that policy was deployed) */
+    tpm_rc = grub_tpm2_get_capability (TPM_CAP_TPM_PROPERTIES,
+                                       TPM_PT_PERMANENT, 1,
+                                       &more, &capability_data);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
-    if(!(capability_data.data.tpmProperties.tpmProperty[0].value & TPMA_PERMANENT_OWNERAUTHSET))
+    if (!(capability_data.data.tpmProperties.tpmProperty[0].value &
+          0x00000001 /* TPMA_PERMANENT_OWNERAUTHSET */))
     {
         result = TSS2_APP_RC_POLICY_NOT_EXIST;
         goto end;
     }
 
-    //2. check soft key state
-    grub_memset(&capability_data, 0, sizeof(TPMS_CAPABILITY_DATA));
-    tpm_rc = grub_tpm2_get_capability(TPM_CAP_HANDLES, SOFTWARE_POLICY_KEY, MAX_CAP_HANDLES, &more, &capability_data);
-    if(tpm_rc)
+    /* 2. Check software policy key handle exists */
+    grub_memset (&capability_data, 0, sizeof (TPMS_CAPABILITY_DATA));
+    tpm_rc = grub_tpm2_get_capability (TPM_CAP_HANDLES, SOFTWARE_POLICY_KEY,
+                                       MAX_CAP_HANDLES, &more, &capability_data);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
-    for(count = 0; count < capability_data.data.handles.count; count++)
+    for (count = 0; count < capability_data.data.handles.count; count++)
     {
-        if((SOFTWARE_POLICY_KEY == capability_data.data.handles.handle[count]))
+        if (capability_data.data.handles.handle[count] == (UINT32)SOFTWARE_POLICY_KEY)
         {
             soft_key_exist = 1;
             break;
         }
     }
-    if(!soft_key_exist)
+    if (!soft_key_exist)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
 
-    //3. check hard key state
-    grub_memset(&capability_data, 0, sizeof(TPMS_CAPABILITY_DATA));
-    tpm_rc = grub_tpm2_get_capability(TPM_CAP_HANDLES, HARDWARE_POLICY_KEY, MAX_CAP_HANDLES, &more, &capability_data);
-    if(tpm_rc)
+    /* 3. Check hardware policy key handle exists */
+    grub_memset (&capability_data, 0, sizeof (TPMS_CAPABILITY_DATA));
+    tpm_rc = grub_tpm2_get_capability (TPM_CAP_HANDLES, HARDWARE_POLICY_KEY,
+                                       MAX_CAP_HANDLES, &more, &capability_data);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
-    for(count = 0; count < capability_data.data.handles.count; count++)
+    for (count = 0; count < capability_data.data.handles.count; count++)
     {
-        if((HARDWARE_POLICY_KEY == capability_data.data.handles.handle[count]))
+        if (capability_data.data.handles.handle[count] == (UINT32)HARDWARE_POLICY_KEY)
         {
             hard_key_exist = 1;
             break;
         }
     }
-    if(!hard_key_exist)
+    if (!hard_key_exist)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
 
-    //4. check nv state
-    grub_memset(&capability_data, 0, sizeof(TPMS_CAPABILITY_DATA));
-    tpm_rc = grub_tpm2_get_capability(TPM_CAP_HANDLES, NV_INDEX_FIRST, MAX_CAP_HANDLES, &more, &capability_data);
-    if(tpm_rc)
+    /* 4. Check policy state NV index exists */
+    grub_memset (&capability_data, 0, sizeof (TPMS_CAPABILITY_DATA));
+    tpm_rc = grub_tpm2_get_capability (TPM_CAP_HANDLES, NV_INDEX_FIRST,
+                                       MAX_CAP_HANDLES, &more, &capability_data);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
-    for(count = 0; count < capability_data.data.handles.count; count++)
+    for (count = 0; count < capability_data.data.handles.count; count++)
     {
-        if((POLICY_STATE_NV == capability_data.data.handles.handle[count]))
+        if (capability_data.data.handles.handle[count] == (UINT32)POLICY_STATE_NV)
         {
             nv_exist = 1;
             break;
         }
     }
-    if(!nv_exist)
+    if (!nv_exist)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
 
+    /* 5. Read the policy state value from NV */
     session.sessionHandle = TPM_RS_PW;
     session.nonce.size = 0;
-    grub_memset(session.nonce.buffer, 0, sizeof(TPMU_HA));
-    grub_memset(&session.sessionAttributes, 0, sizeof(TPMA_SESSION));
+    grub_memset (session.nonce.buffer, 0, sizeof (TPMU_HA));
+    grub_memset (&session.sessionAttributes, 0, sizeof (TPMA_SESSION));
     session.hmac.size = 0;
-    grub_memset(session.hmac.buffer, 0, sizeof(TPMU_HA));
-    //4. check nv content
-    tpm_rc = grub_tpm2_nv_read(POLICY_STATE_NV, POLICY_STATE_NV, &session, 4, 0, &out);
-    if(tpm_rc)
+    grub_memset (session.hmac.buffer, 0, sizeof (TPMU_HA));
+
+    tpm_rc = grub_tpm2_nv_read (POLICY_STATE_NV, POLICY_STATE_NV,
+                                 &session, 4, 0, &out);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
-    unmarshal_uint32(out.buffer, &policy_state);
-    if(AUDIT_POLICY_EXIST == policy_state)
-    {
+
+    unmarshal_uint32 (out.buffer, &policy_state);
+    if (AUDIT_POLICY_EXIST == policy_state)
         result = TSS2_APP_RC_AUDIT_POLICY_EXIST;
-    }
-    else if(SUPERVISORY_POLICY_EXIST == policy_state)
-    {
+    else if (SUPERVISORY_POLICY_EXIST == policy_state)
         result = TSS2_APP_RC_SUPERVISORY_POLICY_EXIST;
-    }
     else
-    {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
-    }
 
 end:
     return result;
 }
+
+/*
+ * check_file_policy_state - read /boot/policy.bin and return the policy type.
+ * Used as fallback when the TPM is not available.
+ */
 grub_err_t
-check_file_policy_state(void)
+check_file_policy_state (void)
 {
-    grub_err_t          result = 0;
-    grub_file_t         file = 0;
-    char                *buf = 0;
-    int                 len;
-    int                 policy_state = 0;
+    grub_err_t   result = TSS2_APP_RC_POLICY_NOT_EXIST;
+    grub_file_t  file = 0;
+    char        *buf = 0;
+    int          len;
+    int          policy_state = 0;
 
-    file = grub_file_open(POLICYFILE, GRUB_FILE_TYPE_CAT);
-    if ((!file) || (!file->size))
+    file = grub_file_open (POLICYFILE, GRUB_FILE_TYPE_CAT);
+    if (!file || !file->size)
     {
         result = TSS2_APP_RC_POLICY_NOT_EXIST;
         goto end;
     }
+
     len = file->size;
     buf = grub_malloc (len + 1);
-
-    if(grub_file_read (file, buf, len) != len)
+    if (!buf)
     {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
         goto end;
     }
 
-    unmarshal_uint32((unsigned char *)buf, (unsigned int *)&policy_state);
-    if(AUDIT_POLICY_EXIST == policy_state)
+    if (grub_file_read (file, buf, len) != len)
     {
-        result = TSS2_APP_RC_AUDIT_POLICY_EXIST;
+        result = TSS2_APP_RC_BAD_POLICY_STATE;
+        goto end;
     }
-    else if(SUPERVISORY_POLICY_EXIST == policy_state)
-    {
+
+    unmarshal_uint32 ((unsigned char *)buf, (unsigned int *)&policy_state);
+    if (AUDIT_POLICY_EXIST == policy_state)
+        result = TSS2_APP_RC_AUDIT_POLICY_EXIST;
+    else if (SUPERVISORY_POLICY_EXIST == policy_state)
         result = TSS2_APP_RC_SUPERVISORY_POLICY_EXIST;
-    }
-    else if(POLICY_NOT_EXIST == policy_state)
-    {
+    else if (POLICY_NOT_EXIST == policy_state)
         result = TSS2_APP_RC_POLICY_NOT_EXIST;
-    }
     else
-    {
         result = TSS2_APP_RC_BAD_POLICY_STATE;
-    }
 
 end:
-    if(buf)
-    {
-        grub_free(buf);
-    }
-
+    if (buf)
+        grub_free (buf);
     if (file)
-    {
-        grub_file_close(file);
-    }
-
-    //grub_printf("check_file_policy_state result %d.\n", result);
+        grub_file_close (file);
     return result;
 }
 
+/* -------------------------------------------------------------------------
+ * Encrypted state file reading
+ * ---------------------------------------------------------------------- */
+
+/*
+ * get_hardware_enc_content - read /boot/hardware_state.bin into caller buffer.
+ * The caller must free *out.
+ */
 grub_err_t
-get_hardware_enc_content(unsigned char **out, int *out_len)
+get_hardware_enc_content (unsigned char **out, int *out_len)
 {
-    grub_err_t          result = 0;
-    grub_file_t         file = 0;
+    grub_err_t  result = GRUB_ERR_NONE;
+    grub_file_t file;
 
-    file = grub_file_open(HARDWAREFILE, GRUB_FILE_TYPE_CAT);
-    if ((file == 0) || (file->size == 0))
+    file = grub_file_open (HARDWAREFILE, GRUB_FILE_TYPE_CAT);
+    if (!file || !file->size)
     {
-#if 0
-        if(file)
-        {
-            grub_printf("%s size is %d.\n", HARDWAREFILE, file->size);
-        }
-        else
-        {
-            grub_printf("%s not exist.\n", HARDWAREFILE);
-        }
-#endif
         result = TSS2_APP_RC_BAD_NO_ENC_FILE;
         goto end;
     }
-    *out_len = file->size;
-    *out = grub_malloc(*out_len);
 
-    //grub_printf("hardware state file size : %d.\n", *out_len);
-
-    if(grub_file_read (file, *out, *out_len) != *out_len)
+    *out_len = file->size;
+    *out = grub_malloc (*out_len);
+    if (!*out)
     {
-        result = TSS2_APP_RC_BAD_NO_ENC_FILE;
+        result = GRUB_ERR_OUT_OF_MEMORY;
         goto end;
     }
 
-    goto end;
-
-    if(*out)
+    if (grub_file_read (file, *out, *out_len) != *out_len)
     {
-        grub_free(*out);
+        grub_free (*out);
         *out = NULL;
         *out_len = 0;
+        result = TSS2_APP_RC_BAD_NO_ENC_FILE;
     }
-end:
 
+end:
     if (file)
-    {
-        grub_file_close(file);
-    }
-    //grub_printf("get_hardware_enc_content done: %d\n", result);
+        grub_file_close (file);
     return result;
 }
+
+/*
+ * get_software_enc_content - read /boot/software_state.bin into caller buffer.
+ * The caller must free *out.
+ */
 grub_err_t
-get_software_enc_content(unsigned char **out, int *out_len)
+get_software_enc_content (unsigned char **out, int *out_len)
 {
-    grub_err_t          result = 0;
-    grub_file_t         file = 0;
+    grub_err_t  result = GRUB_ERR_NONE;
+    grub_file_t file;
 
-    file = grub_file_open(SOFTWAREFILE, GRUB_FILE_TYPE_CAT);
-    if ((!file) || (!file->size))
+    file = grub_file_open (SOFTWAREFILE, GRUB_FILE_TYPE_CAT);
+    if (!file || !file->size)
     {
         result = TSS2_APP_RC_BAD_NO_ENC_FILE;
         goto end;
     }
-    *out_len = (int)file->size;
-    *out = grub_malloc(*out_len + 1);
-    grub_memset(*out, 0, *out_len + 1);
-    if(grub_file_read (file, (void *)*out, *out_len) != *out_len)
+
+    *out_len = file->size;
+    *out = grub_zalloc (*out_len + 1);
+    if (!*out)
     {
-        result = TSS2_APP_RC_BAD_NO_ENC_FILE;
-        goto err;
+        result = GRUB_ERR_OUT_OF_MEMORY;
+        goto end;
     }
 
-    goto end;
-
-err:
-    if(*out)
+    if (grub_file_read (file, *out, *out_len) != *out_len)
     {
-        grub_free(*out);
+        grub_free (*out);
         *out = NULL;
         *out_len = 0;
+        result = TSS2_APP_RC_BAD_NO_ENC_FILE;
     }
 
 end:
     if (file)
-    {
-        grub_file_close(file);
-        file = 0;
-    }
-    //grub_printf("get_software_enc_content done: %d\n", result);
+        grub_file_close (file);
     return result;
 }
 
+/* -------------------------------------------------------------------------
+ * TPM2 key type table for digest sizes
+ * ---------------------------------------------------------------------- */
+
 typedef struct {
     TPM_ALG_ID  algId;
-    UINT16      size;  // Size of digest
-} HASH_SIZE_INFO;
-
-HASH_SIZE_INFO   hash_sizes[] = {
-    {TPM_ALG_SHA1,          SHA1_DIGEST_SIZE},
-    {TPM_ALG_SHA256,        SHA256_DIGEST_SIZE},
-#ifdef TPM_ALG_SHA384
-    {TPM_ALG_SHA384,        SHA384_DIGEST_SIZE},
-#endif
-#ifdef TPM_ALG_SHA512
-    {TPM_ALG_SHA512,        SHA512_DIGEST_SIZE},
+    grub_uint16_t size;
+} hash_size_info_t;
+
+static hash_size_info_t hash_sizes[] = {
+    { TPM_ALG_SHA1,    SHA1_DIGEST_SIZE   },
+    { TPM_ALG_SHA256,  SHA256_DIGEST_SIZE },
+#ifdef TPM_ALG_SM3_256
+    { TPM_ALG_SM3_256, SM3_256_DIGEST_SIZE },
 #endif
-    {TPM_ALG_SM3_256,       SM3_256_DIGEST_SIZE},
-    {TPM_ALG_NULL,0}
+    { TPM_ALG_NULL,    0 }
 };
 
 static grub_uint16_t
-get_digest_size(TPM_ALG_ID auth_hash)
+get_digest_size (TPM_ALG_ID auth_hash)
 {
-    int  i;
-    for(i = 0; i < (int)((sizeof(hash_sizes) / sizeof(HASH_SIZE_INFO))); i++ )
-    {
-        if( hash_sizes[i].algId == auth_hash )
+    int i;
+    for (i = 0; hash_sizes[i].algId != TPM_ALG_NULL; i++)
+        if (hash_sizes[i].algId == auth_hash)
             return hash_sizes[i].size;
-    }
     return 0;
 }
 
+/* -------------------------------------------------------------------------
+ * Hash algorithm selection (prefer SM3 > SHA256 > SHA1)
+ * ---------------------------------------------------------------------- */
 
 static grub_err_t
-check_tpm_hash(const char *hash_alg)
+check_tpm_hash (TPM_ALG_ID alg)
 {
-    grub_err_t              tpm_rc = 0;
-    grub_err_t              result = 0;
-    TPMI_YES_NO             more;
-    TPMS_CAPABILITY_DATA    capability_data;
-    TPM_ALG_ID              alg;
-    int                     len = 0;
-    int                     support = 0;// 0 means false
-    unsigned int            i = 0;
-
-    if(!hash_alg)
-    {
-        result = TSS2_APP_RC_BAD_HASH;
-        goto end;
-    }
-
-    len = grub_strlen(hash_alg);
-    if(len < 3) //sm3 is the shortest, is 3
-    {
-        result = TSS2_APP_RC_BAD_HASH;
-        goto end;
-    }
+    grub_err_t           tpm_rc;
+    TPMI_YES_NO          more;
+    TPMS_CAPABILITY_DATA capability_data;
+    unsigned int         i;
 
-    if(grub_strncasecmp(hash_alg, "sha1", grub_strlen(hash_alg)) == 0)
-    {
-        alg = TPM_ALG_SHA1;
-    }
-    else if(grub_strncasecmp(hash_alg, "sha256", grub_strlen(hash_alg)) == 0)
-    {
-        alg = TPM_ALG_SHA256;
-    }
-    else if(grub_strncasecmp(hash_alg, "sm3", grub_strlen(hash_alg)) == 0)
-    {
-        alg = TPM_ALG_SM3_256;
-    }
-    else
-    {
-        result = TSS2_APP_RC_BAD_HASH;
-        goto end;
-    }
+    tpm_rc = grub_tpm2_get_capability (TPM_CAP_ALGS, 0, 64,
+                                       &more, &capability_data);
+    if (tpm_rc)
+        return TSS2_APP_RC_BAD_HASH;
 
-    tpm_rc = grub_tpm2_get_capability(TPM_CAP_ALGS, 0, 64, &more, &capability_data);
-    if(tpm_rc)
-    {
-        result = TSS2_APP_RC_BAD_HASH;
-        goto end;
-    }
+    for (i = 0; i < capability_data.data.algorithms.count; i++)
+        if (capability_data.data.algorithms.algProperties[i].alg == alg)
+            return GRUB_ERR_NONE;
 
-    for(i = 0; i < capability_data.data.algorithms.count; i++)
-    {
-        //grub_printf("capability hash alg:%04x\n", capability_data.data.algorithms.algProperties[i].alg);
-        if(alg == capability_data.data.algorithms.algProperties[i].alg)
-        {
-            support = 1; //1 means true
-            break;
-        }
-    }
-
-    if(!support)
-    {
-        result = TSS2_APP_RC_BAD_HASH;
-        goto end;
-    }
-
-end:
-    return result;
+    return TSS2_APP_RC_BAD_HASH;
 }
 
 static grub_err_t
-check_tpm_pcr_bank(const char *hash_alg)
+check_tpm_pcr_bank (TPM_ALG_ID alg)
 {
-    grub_err_t              tpm_rc = 0;
-    grub_err_t              result = 0;
-    TPMI_YES_NO             more;
-    TPMS_CAPABILITY_DATA    capability_data;
-    //TPMS_AUTH_COMMAND       session;
-    int                     len = 0;
-    //int                     support = 0;// 0 means false
-    unsigned int            i = 0;
-    int                     j = 0;
-    int                     find = 0;
-    int                     active = 1;
-    unsigned char           mask = 0xff;
-    TPM_ALG_ID              alg;
-    TPMS_PCR_SELECTION      selection;
-
-    if(!hash_alg)
-    {
-        result = TSS2_APP_RC_BAD_PCR_BANK;
-        goto end;
-    }
-
-    len = grub_strlen(hash_alg);
-    if(len < 3) //sm3 is the shortest, is 3
-    {
-        result = TSS2_APP_RC_BAD_PCR_BANK;
-        goto end;
-    }
+    grub_err_t           tpm_rc;
+    TPMI_YES_NO          more;
+    TPMS_CAPABILITY_DATA capability_data;
+    TPMS_PCR_SELECTION   selection;
+    unsigned int         i;
+    int                  j, find = 0, active = 1;
 
-    if(grub_strncasecmp(hash_alg, "sha1", grub_strlen(hash_alg)) == 0)
-    {
-        alg = TPM_ALG_SHA1;
-    }
-    else if(grub_strncasecmp(hash_alg, "sha256", grub_strlen(hash_alg)) == 0)
-    {
-        alg = TPM_ALG_SHA256;
-    }
-    else if(grub_strncasecmp(hash_alg, "sm3", grub_strlen(hash_alg)) == 0)
-    {
-        alg = TPM_ALG_SM3_256;
-    }
-    else
-    {
-        result = TSS2_APP_RC_BAD_PCR_BANK;
-        goto end;
-    }
-
-    tpm_rc = grub_tpm2_get_capability(TPM_CAP_PCRS, 0, 64, &more, &capability_data);
-    if(tpm_rc)
-    {
-        result = TSS2_APP_RC_BAD_PCR_BANK;
-        goto end;
-    }
+    tpm_rc = grub_tpm2_get_capability (TPM_CAP_PCRS, 0, 64,
+                                       &more, &capability_data);
+    if (tpm_rc)
+        return TSS2_APP_RC_BAD_PCR_BANK;
 
-    for(i = 0; i < capability_data.data.assignedPCR.count; i++)
+    for (i = 0; i < capability_data.data.assignedPCR.count; i++)
     {
         selection = capability_data.data.assignedPCR.pcrSelections[i];
-        //grub_printf("capability pcr alg:%04x\n", selection.hash);
-        if(alg == selection.hash)
+        if (selection.hash == alg)
         {
             find = 1;
             break;
         }
     }
 
-    if(find)
+    if (!find)
+        return TSS2_APP_RC_BAD_PCR_BANK;
+
+    for (j = 0; j < selection.sizeofSelect; j++)
     {
-        for(j = 0; j < selection.sizeofSelect; j++)
+        if ((selection.pcrSelect[j] & 0xff) != 0xff)
         {
-            if(mask == (selection.pcrSelect[j] & mask))
-            {
-                continue;
-            }
-            else
-            {
-               active = 0;
-               break;
-            }
+            active = 0;
+            break;
         }
     }
-    else
-    {
-        result = TSS2_APP_RC_BAD_PCR_BANK;
-        goto end;
-    }
 
-    if(active)
-    {
-        result = 0;
-    }
-    else
-    {
-        result = TSS2_APP_RC_BAD_PCR_BANK;
-    }
-
-end:
-    return result;
+    return active ? GRUB_ERR_NONE : TSS2_APP_RC_BAD_PCR_BANK;
 }
+
 static grub_err_t
-get_tpm_supported_hash(TPMI_ALG_HASH *hash_alg)
+get_tpm_supported_hash (TPMI_ALG_HASH *hash_alg)
 {
-    struct hash_check{
-        const char      *hash_alg;
-        int             support;
-        int             active;
-        TPMI_ALG_HASH   alg;
-    }                   hash[3] = { {"sm3", 0, 0, TPM_ALG_SM3_256}, {"sha256", 0, 0, TPM_ALG_SHA256}, {"sha1", 0, 0, TPM_ALG_SHA1}};
-    //grub_err_t          tpm_rc = 0;
-    grub_err_t          result = 0;
-    int                 i = 0;
-
-    //1. find supported hash alg , sm3 > sha256 > sha1
-    for(i = 0; i < (int)(sizeof(hash) / sizeof(hash[0])); i++)
-    {
-        result = check_tpm_hash(hash[i].hash_alg);
-        if(result != 0)
-        {
-            continue;
-        }
+    struct { TPM_ALG_ID alg; int support; int active; } candidates[] = {
+#ifdef TPM_ALG_SM3_256
+        { TPM_ALG_SM3_256, 0, 0 },
+#endif
+        { TPM_ALG_SHA256,  0, 0 },
+        { TPM_ALG_SHA1,    0, 0 },
+    };
+    int i, n = (int)(sizeof (candidates) / sizeof (candidates[0]));
 
-        hash[i].support = 1;
-    }
+    for (i = 0; i < n; i++)
+        if (check_tpm_hash (candidates[i].alg) == GRUB_ERR_NONE)
+            candidates[i].support = 1;
 
-    //2. find active hash bank
-    for(i = 0; i < (int)(sizeof(hash) / sizeof(hash[0])); i++)
+    for (i = 0; i < n; i++)
     {
-        if(hash[i].support)
+        if (!candidates[i].support)
+            continue;
+        if (check_tpm_pcr_bank (candidates[i].alg) == GRUB_ERR_NONE)
         {
-            result = check_tpm_pcr_bank(hash[i].hash_alg);
-            if(result != 0)
-            {
-                continue;
-            }
-            hash[i].active = 1;
-            *hash_alg = (unsigned short)hash[i].alg;
-            break;
+            candidates[i].active = 1;
+            *hash_alg = (TPMI_ALG_HASH)candidates[i].alg;
+            return GRUB_ERR_NONE;
         }
     }
 
-    if(hash_alg == 0)
-    {
-        result = TSS2_APP_RC_BAD_HASH;
-        goto end;
-    }
-
-end:
-    return result;
+    return TSS2_APP_RC_BAD_HASH;
 }
 
+/* -------------------------------------------------------------------------
+ * TPM2 Unseal operations
+ * Sequence: StartAuthSession → PolicyPCR → Unseal → FlushContext
+ * ---------------------------------------------------------------------- */
 
+/*
+ * get_hardware_dec_key - unseal the hardware decryption key from the TPM.
+ *
+ * The key is sealed to PCRs 0,1,2,3,6,7 under handle 0x81010101.
+ * If current PCR values match the policy digest recorded at deploy time,
+ * the TPM releases the key; otherwise Unseal fails.
+ *
+ * The caller must free *out.
+ */
 grub_err_t
-get_hardware_dec_key(unsigned char **out, int *out_len)
+get_hardware_dec_key (unsigned char **out, int *out_len)
 {
-    grub_err_t                result = 0;
-    grub_err_t                tpm_rc = 0;
-    TPMI_DH_OBJECT            tpm_key = TPM_RH_NULL;
-    TPMI_DH_ENTITY            bind = TPM_RH_NULL;
-    TPM2B_NONCE               nonce_caller;
-    TPM2B_ENCRYPTED_SECRET    salt;
-    TPM_SE                    session_type = TPM_SE_POLICY;
-    TPMT_SYM_DEF              symmetric;
-    TPMI_ALG_HASH             auth_hash;
-    TPMI_SH_AUTH_SESSION      session_handle;
-    TPM2B_NONCE               nonce_tpm;
-    grub_uint16_t             digest_size;
-    int                       i;
-    TPM2B_DIGEST              pcr_digest;
-    //TPM2B_DIGEST              test_digest;
-    TPML_PCR_SELECTION        pcrs;
-    int                       pcr_number[8] = {0, 1, 2, 3, 6, 7, -1};
-    TPM2B_SENSITIVE_DATA      out_data;
-    TPMS_AUTH_COMMAND         session;
-#if 0
-    int                       i_test = 0;
-    TPML_PCR_SELECTION        pcrs_test;
-    TPML_PCR_SELECTION        pcrs_test_out;
-    TPML_DIGEST               pcr_value;
-    grub_uint32_t             pcr_update_counter = 0;
-#endif
-    //1. get supported hash
-    tpm_rc = get_tpm_supported_hash(&auth_hash);
-    if(tpm_rc != 0)
+    grub_err_t            result = GRUB_ERR_NONE;
+    grub_err_t            tpm_rc;
+    TPMI_DH_OBJECT        tpm_key = TPM_RH_NULL;
+    TPMI_DH_ENTITY        bind = TPM_RH_NULL;
+    TPM2B_NONCE           nonce_caller;
+    TPM2B_ENCRYPTED_SECRET salt;
+    TPM_SE                session_type = TPM_SE_POLICY;
+    TPMT_SYM_DEF          symmetric;
+    TPMI_ALG_HASH         auth_hash;
+    TPMI_SH_AUTH_SESSION  session_handle;
+    TPM2B_NONCE           nonce_tpm;
+    grub_uint16_t         digest_size;
+    int                   i;
+    TPM2B_DIGEST          pcr_digest;
+    TPML_PCR_SELECTION    pcrs;
+    int                   pcr_number[] = { 0, 1, 2, 3, 6, 7, -1 };
+    TPM2B_SENSITIVE_DATA  out_data;
+    TPMS_AUTH_COMMAND     session;
+
+    /* 1. Select the best supported hash algorithm */
+    tpm_rc = get_tpm_supported_hash (&auth_hash);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
 
-    //2. start auth session
-    digest_size = get_digest_size(auth_hash);
+    /* 2. Start a Policy auth session */
+    digest_size = get_digest_size (auth_hash);
     nonce_caller.size = digest_size;
     salt.size = 0;
-    symmetric.algorithm =TPM_ALG_NULL;
-    grub_memset(nonce_caller.buffer, 0xA5, digest_size);
-    tpm_rc = grub_tpm2_start_auth_session(tpm_key, bind, &nonce_caller, &salt, session_type, &symmetric, auth_hash, &session_handle, &nonce_tpm);
-    if(tpm_rc != 0)
+    symmetric.algorithm = TPM_ALG_NULL;
+    grub_memset (nonce_caller.buffer, 0xA5, digest_size);
+
+    tpm_rc = grub_tpm2_start_auth_session (tpm_key, bind,
+                                            &nonce_caller, &salt,
+                                            session_type, &symmetric,
+                                            auth_hash,
+                                            &session_handle, &nonce_tpm);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
 
-    //3. policypcr
+    /* 3. Bind the session to PCRs 0,1,2,3,6,7 via PolicyPCR */
     pcr_digest.size = 0;
     pcrs.count = 1;
     pcrs.pcrSelections[0].hash = auth_hash;
     pcrs.pcrSelections[0].sizeofSelect = 3;
-    CLEAR_PCR_SELECT_BITS(pcrs.pcrSelections[0]);
+    CLEAR_PCR_SELECT_BITS (pcrs.pcrSelections[0]);
 
-    for(i=0; pcr_number[i] != -1; i++)
-    {
-        if(i > 24)
-        {
-            return TSS2_APP_RC_BAD_TRUST_STATE;
-        }
+    for (i = 0; pcr_number[i] != -1; i++)
+        SET_PCR_SELECT_BIT (pcrs.pcrSelections[0], pcr_number[i]);
 
-        if(pcr_number[i] < 24 && pcr_number[i] >= 0)
-        {
-            SET_PCR_SELECT_BIT(pcrs.pcrSelections[0], pcr_number[i]);
-        }
-        else
-        {
-            result = TSS2_APP_RC_BAD_TRUST_STATE;
-            goto end;
-        }
-    }
-    tpm_rc= grub_tpm2_policy_pcr(session_handle, &pcr_digest, &pcrs);
-    if(tpm_rc != 0)
+    tpm_rc = grub_tpm2_policy_pcr (session_handle, &pcr_digest, &pcrs);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto flush;
     }
 
-#if 0
-    grub_tpm2_policy_get_digest(session_handle, &test_digest);
-
-    for(i_test = 0; pcr_number[i_test] != -1; i_test++)
-    {
-        pcrs_test.count = 1;
-        pcrs_test.pcrSelections[0].hash = auth_hash;
-        pcrs_test.pcrSelections[0].sizeofSelect = 3;
-        CLEAR_PCR_SELECT_BITS(pcrs_test.pcrSelections[0]);
-        SET_PCR_SELECT_BIT(pcrs_test.pcrSelections[0], pcr_number[i_test]);
-        grub_tpm2_pcr_read(&pcrs_test, &pcr_update_counter, &pcrs_test_out, &pcr_value);
-    }
-#endif
-    //4. unseal
+    /* 4. Unseal the hardware key using the policy session */
     session.sessionHandle = session_handle;
     session.nonce.size = 0;
-    grub_memset(session.nonce.buffer, 0, sizeof(TPMU_HA));
-    grub_memset(&session.sessionAttributes, 0, sizeof(TPMA_SESSION));
+    grub_memset (session.nonce.buffer, 0, sizeof (TPMU_HA));
+    grub_memset (&session.sessionAttributes, 0, sizeof (TPMA_SESSION));
     session.hmac.size = 0;
-    grub_memset(session.hmac.buffer, 0, sizeof(TPMU_HA));
-    tpm_rc = grub_tpm2_unseal(HARDWARE_POLICY_KEY, &session, &out_data);
-    if(tpm_rc != 0)
+    grub_memset (session.hmac.buffer, 0, sizeof (TPMU_HA));
+
+    tpm_rc = grub_tpm2_unseal (HARDWARE_POLICY_KEY, &session, &out_data);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto flush;
     }
 
     *out_len = out_data.size;
-    *out = grub_malloc(*out_len);
-    grub_memset(*out, 0, *out_len);
-    grub_memcpy(*out, out_data.buffer, out_data.size);
+    *out = grub_zalloc (*out_len);
+    if (!*out)
+    {
+        result = GRUB_ERR_OUT_OF_MEMORY;
+        goto flush;
+    }
+    grub_memcpy (*out, out_data.buffer, out_data.size);
     goto end;
 
 flush:
-    grub_tpm2_flush_context(session_handle);
-
+    grub_tpm2_flush_context (session_handle);
 end:
-    //grub_printf("get_hardware_dec_key done : %d\n", result);
     return result;
 }
+
+/*
+ * get_software_dec_key - unseal the software decryption key from the TPM.
+ *
+ * The key is sealed to PCRs 8,9,12 under handle 0x81010100.
+ * The caller must free *out.
+ */
 grub_err_t
-get_software_dec_key(unsigned char **out, int *out_len)
+get_software_dec_key (unsigned char **out, int *out_len)
 {
-    grub_err_t                result = 0;
-    grub_err_t                tpm_rc = 0;
-    TPMI_DH_OBJECT            tpm_key = TPM_RH_NULL;
-    TPMI_DH_ENTITY            bind = TPM_RH_NULL;
-    TPM2B_NONCE               nonce_caller;
-    TPM2B_ENCRYPTED_SECRET    salt;
-    TPM_SE                    session_type = TPM_SE_POLICY;
-    TPMT_SYM_DEF              symmetric;
-    TPMI_ALG_HASH             auth_hash;
-    TPMI_SH_AUTH_SESSION      session_handle;
-    TPM2B_NONCE               nonce_tpm;
-    grub_uint16_t             digest_size;
-    int                       i;
-    TPM2B_DIGEST              pcr_digest;
-    //TPM2B_DIGEST              test_digest;
-    TPML_PCR_SELECTION        pcrs;
-    int                       pcr_number[4] = {8, 9, 12, -1};
-    TPM2B_SENSITIVE_DATA      out_data;
-    TPMS_AUTH_COMMAND         session;
-
-    //1. get supported hash
-    tpm_rc = get_tpm_supported_hash(&auth_hash);
-    if(tpm_rc != 0)
+    grub_err_t            result = GRUB_ERR_NONE;
+    grub_err_t            tpm_rc;
+    TPMI_DH_OBJECT        tpm_key = TPM_RH_NULL;
+    TPMI_DH_ENTITY        bind = TPM_RH_NULL;
+    TPM2B_NONCE           nonce_caller;
+    TPM2B_ENCRYPTED_SECRET salt;
+    TPM_SE                session_type = TPM_SE_POLICY;
+    TPMT_SYM_DEF          symmetric;
+    TPMI_ALG_HASH         auth_hash;
+    TPMI_SH_AUTH_SESSION  session_handle;
+    TPM2B_NONCE           nonce_tpm;
+    grub_uint16_t         digest_size;
+    int                   i;
+    TPM2B_DIGEST          pcr_digest;
+    TPML_PCR_SELECTION    pcrs;
+    int                   pcr_number[] = { 8, 9, 12, -1 };
+    TPM2B_SENSITIVE_DATA  out_data;
+    TPMS_AUTH_COMMAND     session;
+
+    /* 1. Select hash algorithm */
+    tpm_rc = get_tpm_supported_hash (&auth_hash);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
 
-    //2. start auth session
-    digest_size = get_digest_size(auth_hash);
+    /* 2. Start Policy auth session */
+    digest_size = get_digest_size (auth_hash);
     nonce_caller.size = digest_size;
     salt.size = 0;
-    symmetric.algorithm =TPM_ALG_NULL;
-    grub_memset(nonce_caller.buffer, 0xA5, digest_size);
-    tpm_rc = grub_tpm2_start_auth_session(tpm_key, bind, &nonce_caller, &salt, session_type, &symmetric, auth_hash, &session_handle, &nonce_tpm);
-    if(tpm_rc != 0)
+    symmetric.algorithm = TPM_ALG_NULL;
+    grub_memset (nonce_caller.buffer, 0xA5, digest_size);
+
+    tpm_rc = grub_tpm2_start_auth_session (tpm_key, bind,
+                                            &nonce_caller, &salt,
+                                            session_type, &symmetric,
+                                            auth_hash,
+                                            &session_handle, &nonce_tpm);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
 
-    //3. policypcr
+    /* 3. Bind to PCRs 8,9,12 */
     pcr_digest.size = 0;
     pcrs.count = 1;
     pcrs.pcrSelections[0].hash = auth_hash;
     pcrs.pcrSelections[0].sizeofSelect = 3;
-    CLEAR_PCR_SELECT_BITS(pcrs.pcrSelections[0]);
+    CLEAR_PCR_SELECT_BITS (pcrs.pcrSelections[0]);
 
-    for(i=0; pcr_number[i] != -1; i++)
-    {
-        if(i > 24)
-        {
-            return TSS2_APP_RC_BAD_TRUST_STATE;
-        }
+    for (i = 0; pcr_number[i] != -1; i++)
+        SET_PCR_SELECT_BIT (pcrs.pcrSelections[0], pcr_number[i]);
 
-        if(pcr_number[i] < 24 && pcr_number[i] >= 0)
-        {
-            SET_PCR_SELECT_BIT(pcrs.pcrSelections[0], pcr_number[i]);
-        }
-        else
-        {
-            result = TSS2_APP_RC_BAD_TRUST_STATE;
-            goto end;
-        }
-    }
-    tpm_rc= grub_tpm2_policy_pcr(session_handle, &pcr_digest, &pcrs);
-    if(tpm_rc != 0)
+    tpm_rc = grub_tpm2_policy_pcr (session_handle, &pcr_digest, &pcrs);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto flush;
     }
-    //grub_tpm2_policy_get_digest(session_handle, &test_digest);
-    //4. unseal
+
+    /* 4. Unseal the software key */
     session.sessionHandle = session_handle;
     session.nonce.size = 0;
-    grub_memset(session.nonce.buffer, 0, sizeof(TPMU_HA));
-    grub_memset(&session.sessionAttributes, 0, sizeof(TPMA_SESSION));
+    grub_memset (session.nonce.buffer, 0, sizeof (TPMU_HA));
+    grub_memset (&session.sessionAttributes, 0, sizeof (TPMA_SESSION));
     session.hmac.size = 0;
-    grub_memset(session.hmac.buffer, 0, sizeof(TPMU_HA));
-    tpm_rc = grub_tpm2_unseal(SOFTWARE_POLICY_KEY, &session, &out_data);
-    if(tpm_rc != 0)
+    grub_memset (session.hmac.buffer, 0, sizeof (TPMU_HA));
+
+    tpm_rc = grub_tpm2_unseal (SOFTWARE_POLICY_KEY, &session, &out_data);
+    if (tpm_rc)
     {
         result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto flush;
     }
 
     *out_len = out_data.size;
-    *out = grub_malloc(*out_len);
-    grub_memset(*out, 0, *out_len);
-    grub_memcpy(*out, out_data.buffer, out_data.size);
+    *out = grub_zalloc (*out_len);
+    if (!*out)
+    {
+        result = GRUB_ERR_OUT_OF_MEMORY;
+        goto flush;
+    }
+    grub_memcpy (*out, out_data.buffer, out_data.size);
     goto end;
 
 flush:
-    grub_tpm2_flush_context(session_handle);
-
+    grub_tpm2_flush_context (session_handle);
 end:
-    //grub_printf("get_software_dec_key done : %d\n", result);
     return result;
 }
-
diff --git a/grub-core/kern/efi/tpm2nvstorage.c b/grub-core/kern/efi/tpm2nvstorage.c
index 1a57b66..f3b3a02 100644
--- a/grub-core/kern/efi/tpm2nvstorage.c
+++ b/grub-core/kern/efi/tpm2nvstorage.c
@@ -184,13 +184,13 @@ again:
             Status = grub_error(GRUB_ERR_BAD_ARGUMENT, N_("TPM2 Nv Read Bad auth entity "));
             break;
         case TPM_RC_NV_LOCKED:
-            Status = grub_error (GRUB_ERR_ACCESS_DENIED, N_("TPM2 Nv Locked"));
+            Status = grub_error (GRUB_ERR_IO, N_("TPM2 Nv Locked"));
             break;
         case TPM_RC_NV_RANGE:
             Status = grub_error (GRUB_ERR_OUT_OF_RANGE, N_("TPM2 Nv Read out of range"));
             break;
         case TPM_RC_NV_UNINITIALIZED:
-            Status = grub_error (GRUB_ERR_BAD_DEVICE, N_("TPM2 Nv Uninitialized"));
+            Status = grub_error (GRUB_ERR_IO, N_("TPM2 Nv Uninitialized"));
             break;
         case TPM_RC_HANDLE + RC_NV_Read_nvIndex:
             Status = grub_error (GRUB_ERR_NOT_IMPLEMENTED_YET, "TPM2 Nv Index Not Defined");
diff --git a/grub-core/kern/efi/tpm2tis.c b/grub-core/kern/efi/tpm2tis.c
index f983d11..f08cf9b 100644
--- a/grub-core/kern/efi/tpm2tis.c
+++ b/grub-core/kern/efi/tpm2tis.c
@@ -9,6 +9,8 @@
 #include <grub/tpm20.h>
 #include <grub/term.h>
 
+/* grub2-2.12 renamed grub_efi_guid_t to grub_guid_t */
+#define grub_efi_guid_t grub_guid_t
 static grub_guid_t tpm2_guid = EFI_TPM2_GUID;
 
 static grub_efi_boolean_t grub_tpm2_handle_find(grub_efi_handle_t *tpm_handle,
@@ -65,7 +67,7 @@ grub_tpm2_submit_command(UINT32 inbufsize,
     grub_efi_status_t             status;
     grub_efi_tpm2_protocol_t      *tpm;
     grub_efi_handle_t             tpm_handle;
-    grub_guid_t                   guid = EFI_TPM2_GUID;
+    grub_efi_guid_t               guid = EFI_TPM2_GUID;
     grub_efi_handle_t             *handles;
     grub_efi_uintn_t              num_handles;
     
diff --git a/grub-core/kern/tlcp.c b/grub-core/kern/tlcp.c
index 5f2b298..0286a94 100644
--- a/grub-core/kern/tlcp.c
+++ b/grub-core/kern/tlcp.c
@@ -1,3 +1,23 @@
+/*
+ *  GRUB  --  GRand Unified Bootloader
+ *  iTrustMidware TLCP trusted boot core for grub2-2.12
+ *
+ *  This file implements the public TLCP API called from commands/boot.c:
+ *    grub_tpm_policy_state()        — determine policy type via TPM
+ *    grub_file_policy_state()       — determine policy type via file
+ *    grub_tpm_integrity_validation()  — validate integrity via TPM Unseal
+ *    grub_file_integrity_validation() — validate integrity via passphrase
+ *    grub_measure_configured_files()  — extend PCR with config file hash
+ *
+ *  State file format (hardware_state.bin / software_state.bin):
+ *    [0..31]  SHA256(decryption_key)          — integrity check
+ *    [32..]   AES-128-CBC(magic_string, key)  — plaintext after decrypt
+ *
+ *  Magic strings (verified after decryption):
+ *    hardware: "INSPUR__HARDWARE"
+ *    software: "INSPUR__SOFTWARE"
+ */
+
 #include <grub/err.h>
 #include <grub/env.h>
 #include <grub/time.h>
@@ -7,10 +27,8 @@
 #include <grub/tpm.h>
 #include <grub/tlcp.h>
 #include <grub/term.h>
-#include <grub/crypto.h>
 #include <grub/tlcp_sha256.h>
 #include <grub/tlcp_aes.h>
-#include <grub/efi/tlcp.h>
 #include <grub/file.h>
 
 #define DEFAULT_KEY_LEN     128
@@ -19,1102 +37,607 @@
 #define SHA256_DIGEST_LEN   32
 #define AES_BLOCK_SIZE      16
 
-#define MEASUREMENT_CONFIG_FILE "/host_st_configure.xml"
-
-grub_err_t tpm_software_integrity_validation(unsigned char *buf, int buf_len);
-int validate_software_key(unsigned char *key, int key_len, unsigned char *buf, int buf_len);
-grub_err_t tpm_hardware_integrity_validation(unsigned char *buf, int buf_len);
-int validate_hardware_key(unsigned char *key, int key_len, unsigned char *buf, int buf_len);
-void sha256(unsigned char *in, int in_len, unsigned char *out, int *out_len);
-void aes_decrypt(unsigned char *key, int key_len, unsigned char *msg, int msg_len, unsigned char *out);
-char hex_char_to_int(char c);
-int hex_string_to_bytes(const char *in, const int size, unsigned char *outbuffer);
-int create_key(unsigned char *passphrase, int passphrase_len, unsigned char **key, int *key_len);
-int convert_passphrase_to_key(char *passphrase, int passphrase_len, unsigned char **key, int *key_len);
-grub_err_t file_software_integrity_validation(unsigned char *buf, int buf_len);
-grub_err_t file_hardware_integrity_validation(unsigned char *buf, int buf_len);
-
-void grub_cls()
+/* Configuration file measured into PCR 12 */
+#define MEASUREMENT_CONFIG_FILE  "/host_st_configure.xml"
+
+/* -------------------------------------------------------------------------
+ * Terminal helpers
+ * ---------------------------------------------------------------------- */
+
+static void
+grub_tlcp_cls (void)
 {
     struct grub_term_output *term;
-
-    FOR_ACTIVE_TERM_OUTPUTS(term)
+    FOR_ACTIVE_TERM_OUTPUTS (term)
     {
-        if ((term->flags & GRUB_TERM_DUMB) || (grub_env_get ("debug")))
-        {
-            //grub_putcode ('\n', term);
+        if ((term->flags & GRUB_TERM_DUMB) || grub_env_get ("debug"))
             grub_term_refresh (term);
-        }
         else
-            (term->cls) (term);
+            term->cls (term);
     }
 }
-int
-grub_password_get (char buf[], unsigned buf_size)
+
+static int
+grub_tlcp_password_get (char buf[], unsigned buf_size)
 {
     unsigned cur_len = 0;
     int key;
-    //char str[4];
-    //int c;
 
     while (1)
     {
         key = grub_getkey ();
         if (key == '\n' || key == '\r')
             break;
-
         if (key == GRUB_TERM_ESC)
         {
             cur_len = 0;
             break;
         }
-
         if (key == '\b' && cur_len)
         {
-            if (cur_len)
-            {
-                cur_len--;
-            }
+            cur_len--;
             continue;
         }
-
         if (!grub_isprint (key))
             continue;
-
         if (cur_len + 2 < buf_size)
-        {
             buf[cur_len++] = key;
-        }
     }
 
     grub_memset (buf + cur_len, 0, buf_size - cur_len);
-
     grub_xputs ("\n");
     grub_refresh ();
-
     return (key != GRUB_TERM_ESC);
 }
 
+/* -------------------------------------------------------------------------
+ * Policy state
+ * ---------------------------------------------------------------------- */
+
 /*
- * @return :
- * */
+ * grub_tpm_policy_state - determine policy state, preferring TPM NV.
+ *
+ * Returns a bitmask:
+ *   POLICY_STATE_NONE         — no policy deployed
+ *   POLICY_STATE_TPM_CHECK | POLICY_STATE_AUDIT       — TPM + audit
+ *   POLICY_STATE_TPM_CHECK | POLICY_STATE_SUPERVISORY — TPM + supervisory
+ *   POLICY_STATE_SUPERVISORY  — file-based supervisory (TPM unavailable)
+ */
 grub_err_t
-grub_tpm_policy_state(void)
+grub_tpm_policy_state (void)
 {
-    int      policy_state_tpm = TSS2_APP_RC_POLICY_NOT_EXIST;
-    int      policy_state_file = TSS2_APP_RC_POLICY_NOT_EXIST;
-
-    policy_state_tpm = check_tpm_policy_state();
-    if(policy_state_tpm == TSS2_APP_RC_AUDIT_POLICY_EXIST || policy_state_tpm == TSS2_APP_RC_SUPERVISORY_POLICY_EXIST)
-    {
-        return policy_state_tpm == TSS2_APP_RC_AUDIT_POLICY_EXIST ? POLICY_STATE_TPM_CHECK + POLICY_STATE_AUDIT : POLICY_STATE_TPM_CHECK + POLICY_STATE_SUPERVISORY;
-    }
-
-    policy_state_file = check_file_policy_state();
-    if(policy_state_file == TSS2_APP_RC_AUDIT_POLICY_EXIST || policy_state_file == TSS2_APP_RC_SUPERVISORY_POLICY_EXIST)
-    {
+    int policy_state_tpm  = TSS2_APP_RC_POLICY_NOT_EXIST;
+    int policy_state_file = TSS2_APP_RC_POLICY_NOT_EXIST;
+
+    policy_state_tpm = check_tpm_policy_state ();
+    if (policy_state_tpm == TSS2_APP_RC_AUDIT_POLICY_EXIST)
+        return POLICY_STATE_TPM_CHECK | POLICY_STATE_AUDIT;
+    if (policy_state_tpm == TSS2_APP_RC_SUPERVISORY_POLICY_EXIST)
+        return POLICY_STATE_TPM_CHECK | POLICY_STATE_SUPERVISORY;
+
+    /* TPM check failed — fall back to policy file */
+    policy_state_file = check_file_policy_state ();
+    if (policy_state_file == TSS2_APP_RC_AUDIT_POLICY_EXIST ||
+        policy_state_file == TSS2_APP_RC_SUPERVISORY_POLICY_EXIST)
         return POLICY_STATE_SUPERVISORY;
-    }
 
-    if(policy_state_file == TSS2_APP_RC_POLICY_NOT_EXIST && policy_state_tpm == TSS2_APP_RC_POLICY_NOT_EXIST)
-    {
+    if (policy_state_file == TSS2_APP_RC_POLICY_NOT_EXIST &&
+        policy_state_tpm  == TSS2_APP_RC_POLICY_NOT_EXIST)
         return POLICY_STATE_NONE;
-    }
-    else
-    {
-        return POLICY_STATE_SUPERVISORY;
-    }
+
+    return POLICY_STATE_SUPERVISORY;
 }
 
+/*
+ * grub_file_policy_state - determine policy state using only /boot/policy.bin.
+ * Used when the TPM is absent.
+ */
 grub_err_t
-grub_file_policy_state(void)
+grub_file_policy_state (void)
 {
-    int      grub_file_policy_state = TSS2_APP_RC_POLICY_NOT_EXIST;
+    int file_state = check_file_policy_state ();
 
-    grub_file_policy_state = check_file_policy_state();
-    if(grub_file_policy_state == TSS2_APP_RC_AUDIT_POLICY_EXIST || grub_file_policy_state == TSS2_APP_RC_SUPERVISORY_POLICY_EXIST)
-    {
+    if (file_state == TSS2_APP_RC_AUDIT_POLICY_EXIST ||
+        file_state == TSS2_APP_RC_SUPERVISORY_POLICY_EXIST)
         return POLICY_STATE_SUPERVISORY;
-    }
 
-    if(grub_file_policy_state == TSS2_APP_RC_POLICY_NOT_EXIST)
-    {
+    if (file_state == TSS2_APP_RC_POLICY_NOT_EXIST)
         return POLICY_STATE_NONE;
-    }
-    else
-    {
-        return POLICY_STATE_SUPERVISORY;
-    }
-}
 
-grub_err_t
-tpm_software_integrity_validation(unsigned char *buf, int buf_len)
-{
-    grub_err_t              result = 0;
-    //char                    passphrase[32] = {0x00, };
-    unsigned char           *key = NULL;
-    int                     key_len = 0;
-
-    //1.get key stored in nv
-    result = get_software_dec_key(&key, &key_len);
-    if(result)
-    {
-        grub_printf(N_("Error Bad Software State.\n"));
-        result = TSS2_APP_RC_BAD_TRUST_STATE;
-        goto end;
-    }
+    return POLICY_STATE_SUPERVISORY;
+}
 
-    //2.validate key
-    result = validate_software_key(key, key_len, buf, buf_len);
-    if(result)
-    {
-        grub_printf(N_("Error Bad Software Passphrase.\n"));
-        result = TSS2_APP_RC_BAD_TRUST_STATE;
-        goto end;
-    }
+/* -------------------------------------------------------------------------
+ * Key derivation from passphrase
+ * ---------------------------------------------------------------------- */
 
-end:
-    if(key)
-    {
-        grub_free(key);
-        key = NULL;
-        key_len = 0;
-    }
-    return result;
+static char
+hex_char_to_int (char c)
+{
+    if (c >= '0' && c <= '9') return c - '0';
+    if (c >= 'A' && c <= 'F') return c - 'A' + 10;
+    if (c >= 'a' && c <= 'f') return c - 'a' + 10;
+    return (char)0xFF;
 }
 
-grub_err_t
-tpm_hardware_integrity_validation(unsigned char *buf, int buf_len)
+static int
+hex_string_to_bytes (const char *in, int size, unsigned char *out)
 {
-    grub_err_t              result;
-    //char                    passphrase[32] = {0x00, };
-    unsigned char           *key = NULL;
-    int                     key_len = 0;
-
-    //1.get key stored in nv
-    result = get_hardware_dec_key(&key, &key_len);
-    if(result)
-    {
-        grub_printf(N_("Error Bad Hardware State.\n"));
-        result = TSS2_APP_RC_BAD_TRUST_STATE;
-        goto end;
-    }
-
-    //2.validate key
-    result = validate_hardware_key(key, key_len, buf, buf_len);
-    if(result)
-    {
-        grub_printf(N_("Error Bad Hardware Passphrase.\n"));
-        result = TSS2_APP_RC_BAD_TRUST_STATE;
-        goto end;
-    }
-end:
-    if(key)
+    int i;
+    for (i = 0; i < size; i += 2)
     {
-        //grub_printf("free key\n");
-        grub_free(key);
-        key = NULL;
-        key_len = 0;
+        char hi = hex_char_to_int (in[i]);
+        char lo = hex_char_to_int (in[i + 1]);
+        if ((unsigned char)hi == 0xFF || (unsigned char)lo == 0xFF)
+            return -1;
+        out[i / 2] = (unsigned char)((hi << 4) | lo);
     }
-
-    //grub_printf("get_hardware_dec_key result %8x.\n", result);
-    return result;
+    return size / 2;
 }
 
-static void edianl_to_b(unsigned int * hashl, unsigned char *hashb, int size)
+static int
+create_key (unsigned char *passphrase, int passphrase_len,
+            unsigned char **key, int *key_len)
 {
-    int i;
-    for(i = 0; i < size; i++)
+    if (passphrase_len <= 0 || !passphrase)
     {
-        hashb[4*i + 0] = (hashl[i] >> 24) & 0xff;
-        hashb[4*i + 1] = (hashl[i] >> 16) & 0xff;
-        hashb[4*i + 2] = (hashl[i] >> 8) & 0xff;
-        hashb[4*i + 3] =  hashl[i] & 0xff;
+        *key = NULL;
+        *key_len = 0;
+        return -1;
     }
+    *key = grub_zalloc (DEFAULT_KEY_LEN);
+    if (!*key)
+        return -1;
+    grub_memcpy (*key, passphrase, passphrase_len);
+    *key_len = DEFAULT_KEY_LEN;
+    return 0;
 }
 
-
-void sha256(unsigned char *in, int in_len, unsigned char *out, int *out_len)
+static int
+convert_passphrase_to_key (char *passphrase, int passphrase_len,
+                            unsigned char **key, int *key_len)
 {
-    unsigned int        data_hash[8] = {0, };
-    sha256_context      context;
-    if(!in || in_len <= 0)
-    {
-        return;
-    }
+    unsigned char passphrase_byte[32] = { 0 };
+    int result;
 
-    sha256_init(&context);
-    sha256_update(&context, (void *)in, (unsigned int)in_len);
-    sha256_finalize(&context, data_hash);
+    if (passphrase_len == 0 || passphrase_len % 2 != 0)
+        return -1;
 
-    edianl_to_b(data_hash, out, sizeof(data_hash) / sizeof(unsigned int));
-    *out_len = SHA256_DIGEST_LEN;
-}
+    result = hex_string_to_bytes (passphrase, passphrase_len, passphrase_byte);
+    if (result < 0)
+        return -1;
 
-void aes_decrypt(unsigned char *key, int key_len, unsigned char *msg, int msg_len, unsigned char *out)
-{
-    AES_CTX                   ctx;
-    const unsigned char       iv[AES_BLOCK_SIZE] = {0x00, };
-
-    (void)key_len;
-#if 0
-    int                       i = 0;
-    grub_printf("msg_len:%d.\n", msg_len);
-    for(i = 0; i < msg_len; i++)
-    {
-        grub_printf("%02x", *(msg + i));
-    }
-    grub_printf("\n");
-#endif
-    AES_set_key(&ctx, key, iv, AES_MODE_128);
-    AES_convert_key(&ctx);
-    AES_cbc_decrypt(&ctx, msg, out, msg_len);
-#if 0
-    for(i = 0;i < msg_len; i++)
-    {
-        grub_printf("%02x", *(out + i));
-    }
-    grub_printf("\n");
-#endif
+    return create_key (passphrase_byte, passphrase_len / 2, key, key_len);
 }
 
-char hex_char_to_int(char c)
-{
-    if(c>='0' && c <='9')
-        return (c-'0');
-    if(c>='A' && c <='F')
-        return (c-'A'+10);
-    if(c>='a' && c <='f')
-        return (c-'a'+10);
-
-    return 0xFF;
-}
+/* -------------------------------------------------------------------------
+ * SHA256 wrapper
+ * ---------------------------------------------------------------------- */
 
-int
-hex_string_to_bytes(const char *in, const int size, unsigned char *outbuffer)
+static void
+tlcp_sha256 (unsigned char *in, int in_len,
+             unsigned char *out, int *out_len)
 {
-    int i = 0;
-    char tmp_first = 0;
-    char tmp_second = 0;
+    unsigned int  data_hash[8] = { 0 };
+    sha256_context context;
+    int           i;
 
-    for(i = 0; i < size; i += 2)
-    {
-        tmp_first = hex_char_to_int(in[i]);
-        tmp_second = hex_char_to_int(in[i + 1]);
-        if(tmp_first == 0xFF || tmp_second == 0xFF)
-        {
-            return -1;
-        }
+    if (!in || in_len <= 0)
+        return;
 
-        outbuffer[i / 2] = ((unsigned char)(tmp_first << 4 | tmp_second));
-    }
-#if 0
-    int count = 0;
-    grub_printf("hex_string_to_bytes:");
-    for(; count < size / 2; count++)
+    sha256_init (&context);
+    sha256_update (&context, in, (unsigned int)in_len);
+    sha256_finalize (&context, data_hash);
+
+    /* Convert from little-endian words to byte array */
+    for (i = 0; i < 8; i++)
     {
-        grub_printf("%02x", outbuffer[count]);
+        out[4*i + 0] = (data_hash[i] >> 24) & 0xff;
+        out[4*i + 1] = (data_hash[i] >> 16) & 0xff;
+        out[4*i + 2] = (data_hash[i] >>  8) & 0xff;
+        out[4*i + 3] =  data_hash[i]        & 0xff;
     }
-    grub_printf("\n");
-#endif
-    return size / 2;
+    *out_len = SHA256_DIGEST_LEN;
 }
-int
-create_key(unsigned char *passphrase, int passphrase_len, unsigned char **key, int *key_len)
-{
-    if(passphrase_len <= 0 || passphrase == NULL)
-    {
-        *key = NULL;
-        *key_len = 0;
-        return -1;
-    }
 
-    *key = grub_malloc(DEFAULT_KEY_LEN);
-    grub_memset(*key, 0, DEFAULT_KEY_LEN);
-    grub_memcpy(*key, passphrase, passphrase_len);
-    *key_len = DEFAULT_KEY_LEN;
+/* -------------------------------------------------------------------------
+ * AES-128-CBC decryption wrapper
+ * ---------------------------------------------------------------------- */
 
-    return 0;
+static void
+tlcp_aes_decrypt (unsigned char *key, int key_len __attribute__((unused)),
+                  unsigned char *msg, int msg_len,
+                  unsigned char *out)
+{
+    AES_CTX         ctx;
+    unsigned char   iv[AES_BLOCK_SIZE] = { 0 };
+
+    AES_set_key (&ctx, key, iv, AES_MODE_128);
+    AES_convert_key (&ctx);
+    AES_cbc_decrypt (&ctx, msg, out, msg_len);
 }
 
+/* -------------------------------------------------------------------------
+ * State file validation
+ *
+ * Format: [SHA256(key) | AES-128-CBC(magic_string, key)]
+ * ---------------------------------------------------------------------- */
 
 /*
- * 1. passphrase must be hex string
- * 2. passphrase length must be even number
- * */
-int
-convert_passphrase_to_key(char *passphrase, int passphrase_len, unsigned char **key, int *key_len)
+ * validate_key_against_state - decrypt state file content with the given key
+ * and verify the magic string.
+ *
+ * Returns 0 on success (integrity OK), -1 on failure.
+ */
+static int
+validate_key_against_state (unsigned char *key, int key_len,
+                             unsigned char *buf, int buf_len,
+                             const char *magic)
 {
-    unsigned char               passphrase_byte[32] = {0x00, };
-    int                         result = 0;
+    unsigned char key_hash[SHA256_DIGEST_LEN] = { 0 };
+    unsigned char out[64] = { 0 };
+    int           hash_len = 0;
 
-    if(passphrase_len == 0 || passphrase_len % 2 != 0)
-    {
+    if (buf_len < SHA256_DIGEST_LEN + AES_BLOCK_SIZE)
         return -1;
-    }
 
-    result = hex_string_to_bytes(passphrase, passphrase_len, passphrase_byte);
-    if(result < 0)
-    {
+    /* Compute SHA256(key) and compare with first 32 bytes of state file */
+    tlcp_sha256 (key, key_len, key_hash, &hash_len);
+    if (grub_memcmp (key_hash, buf, SHA256_DIGEST_LEN) != 0)
         return -1;
-    }
 
-    result = create_key(passphrase_byte, passphrase_len / 2, key, key_len);
-    if(result < 0)
-    {
-        return -1;;
-    }
+    /* Decrypt the remainder and check the magic string */
+    tlcp_aes_decrypt (key, key_len,
+                      buf + SHA256_DIGEST_LEN,
+                      buf_len - SHA256_DIGEST_LEN,
+                      out);
+
+    if (grub_memcmp (magic, out, grub_strlen (magic)) != 0)
+        return -1;
 
     return 0;
 }
 
-int
-validate_software_key(unsigned char *key, int key_len, unsigned char *buf, int buf_len)
-{
-    unsigned char               key_hash[SHA256_DIGEST_LEN] = {0x00, };
-    int                         result = 0;
-    unsigned char               key_tmp[256] = {0x00, };
-    unsigned char               buf_tmp[256] = {0x00, };
-    unsigned char               out[32] = {0x00, };
-    unsigned int                data_hash[8] = {0, };
-    sha256_context              context;
-
-    grub_memcpy((void *)key_tmp, (void *)key, (grub_size_t)key_len);
-    grub_memcpy((void *)buf_tmp, (void *)buf, (grub_size_t)buf_len);
-
-#if 0
-    int                         i = 0;
-    grub_printf("key_len: %d.\n", key_len);
-    for(i = 0; i < key_len; i++)
-    {
-        grub_printf("%02x", key_tmp[i]);
-    }
-    grub_printf("\n");
-#endif
-
-    sha256_init(&context);
-    sha256_update(&context, (void *)key_tmp, (unsigned int)key_len);
-    sha256_finalize(&context, data_hash);
-    edianl_to_b(data_hash, key_hash, sizeof(data_hash) / sizeof(unsigned int));
-#if 0
-    for(i = 0; i < SHA256_DIGEST_LEN; i++)
-    {
-        grub_printf("%02x", key_hash[i]);
-    }
-    grub_printf("\n");
-
-    grub_printf("buf len: %d.\n", buf_len);
-    for(i = 0; i < buf_len; i++)
-    {
-        grub_printf("%02x", buf_tmp[i]);
-    }
-    grub_printf("\n");
-#endif
-    if(grub_memcmp((void *)key_hash, (void *)buf_tmp, SHA256_DIGEST_LEN) != 0)
-    {
-        result = -1;
-        goto end;
-    }
-
-    aes_decrypt(key, key_len, buf_tmp + SHA256_DIGEST_LEN, buf_len - SHA256_DIGEST_LEN, out);
-#if 0
-    grub_printf("soft_out:%s", out);
-#endif
-    if(grub_memcmp(SOFTINFO, (void *)out, grub_strlen(SOFTINFO)) != 0)
-    {
-        result = -1;
-    }
+/* -------------------------------------------------------------------------
+ * TPM-based integrity validation (using TPM2 Unseal)
+ * ---------------------------------------------------------------------- */
 
-end:
-#if 0
-    grub_printf("finish validate_software_key.\n");
-    grub_printf("result:%d.\n", result);
-#endif
-    return result;
-}
-int
-validate_hardware_key(unsigned char *key, int key_len, unsigned char *buf, int buf_len)
+static grub_err_t
+tpm_hardware_integrity_validation (unsigned char *buf, int buf_len)
 {
-    unsigned char               key_hash[SHA256_DIGEST_LEN] = {0x00, };
-    int                         result = 0;
-    unsigned char               key_tmp[256] = {0x00, };
-    unsigned char               buf_tmp[256] = {0x00, };
-    unsigned char               out[32] = {0x00, };
-    unsigned int                data_hash[8] = {0, };
-    sha256_context              context;
-
-    grub_memcpy((void *)key_tmp, (void *)key, (grub_size_t)key_len);
-    grub_memcpy((void *)buf_tmp, (void *)buf, (grub_size_t)buf_len);
-
-#if 0
-    int                         i = 0;
-    grub_printf("key_len: %d.\n", key_len);
-    for(i = 0; i < key_len; i++)
-    {
-        grub_printf("%02x", key_tmp[i]);
-    }
-    grub_printf("\n");
-#endif
-
-    sha256_init(&context);
-    sha256_update(&context, (void *)key_tmp, (unsigned int)key_len);
-    sha256_finalize(&context, data_hash);
-    edianl_to_b(data_hash, key_hash, sizeof(data_hash) / sizeof(unsigned int));
-#if 0
-    for(i = 0; i < SHA256_DIGEST_LEN; i++)
-    {
-        grub_printf("%02x", key_hash[i]);
-    }
-    grub_printf("\n");
-
-    grub_printf("buf len: %d.\n", buf_len);
-    for(i = 0; i < buf_len; i++)
-    {
-        grub_printf("%02x", buf_tmp[i]);
-    }
-    grub_printf("\n");
-#endif
-    if(grub_memcmp((void *)key_hash, (void *)buf_tmp, SHA256_DIGEST_LEN) != 0)
-    {
-        result = -1;
-        goto end;
-    }
-
-    aes_decrypt(key, key_len, buf_tmp + SHA256_DIGEST_LEN, buf_len - SHA256_DIGEST_LEN, out);
-#if 0
-    grub_printf("soft_out:%s", out);
-#endif
-    if(grub_memcmp(HARDINFO, (void *)out, grub_strlen(HARDINFO)) != 0)
-    {
-        result = -1;
-    }
-
-end:
-#if 0
-    grub_printf("finish validate_software_key.\n");
-    grub_printf("result:%d.\n", result);
-#endif
-    return result;
-}
+    unsigned char *key     = NULL;
+    int            key_len = 0;
+    grub_err_t     result;
 
-grub_err_t
-file_software_integrity_validation(unsigned char *buf, int buf_len)
-{
-    grub_err_t        result = 0;
-    char              passphrase[32] = {0x00};
-    unsigned char     *key = NULL;
-    int               key_len = 0;
-#if 0
-    result = get_software_enc_content(&buf, &buf_len);
-    if(result != 0)
+    result = get_hardware_dec_key (&key, &key_len);
+    if (result)
     {
-        grub_printf(N_("Error Bad Software State File.\n"));
+        grub_printf (N_("Error: hardware PCR state mismatch (TPM Unseal failed).\n"));
+        result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
 
-    if(buf_len != grub_strlen(SOFTINFO) + SHA256_DIGEST_LEN)
+    if (validate_key_against_state (key, key_len, buf, buf_len, HARDINFO) != 0)
     {
-        grub_printf(N_("Error Bad Software State File.\n"));
+        grub_printf (N_("Error: hardware state file validation failed.\n"));
+        result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
-#endif
-    while(1)
-    {
-        if(key)
-        {
-            grub_free(key);
-            key = NULL;
-            key_len = 0;
-        }
-        grub_memset(passphrase, 0, sizeof(passphrase));
-        grub_printf(N_("Enter software passphrase "));
-        if (!grub_password_get (passphrase, 32) || grub_strlen(passphrase) == 0)
-        {
-            grub_cls();
-            grub_printf(N_("Error software passphrase not supplied !!!\n"));
-            continue;
-        }
 
-        if(convert_passphrase_to_key(passphrase, grub_strlen(passphrase), &key, &key_len))
-        {
-            grub_cls();
-            grub_printf(N_("Error software passphrase wrong !!!\n"));
-            continue;
-        }
-
-        if(validate_software_key(key, key_len, buf, buf_len))
-        {
-            grub_cls();
-            grub_printf(N_("Error software passphrase wrong !!!\n"));
-            continue;
-        }
-        grub_printf(N_("Passphrase is correct !!!\n"));
-        break;
-    }
-//end:
-    if(key)
+end:
+    if (key)
     {
-        grub_free(key);
+        grub_free (key);
         key = NULL;
-        key_len = 0;
     }
-
     return result;
 }
 
-grub_err_t
-file_hardware_integrity_validation(unsigned char *buf, int buf_len)
+static grub_err_t
+tpm_software_integrity_validation (unsigned char *buf, int buf_len)
 {
-    grub_err_t        result = 0;
-    char              passphrase[32] = {0x00, };
-    unsigned char     *key = NULL;
-    int               key_len = 0;
-#if 0
-    result = get_hardware_enc_content(&buf, &buf_len);
-    if(result != 0)
+    unsigned char *key     = NULL;
+    int            key_len = 0;
+    grub_err_t     result;
+
+    result = get_software_dec_key (&key, &key_len);
+    if (result)
     {
-        grub_printf(N_("Error Bad Hardware State File %08x.\n"), result);
+        grub_printf (N_("Error: software PCR state mismatch (TPM Unseal failed).\n"));
+        result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
 
-    if(buf_len != grub_strlen(HARDINFO) + SHA256_DIGEST_LEN)
+    if (validate_key_against_state (key, key_len, buf, buf_len, SOFTINFO) != 0)
     {
-        grub_printf(N_("Error Bad Hardware State File.\n"));
+        grub_printf (N_("Error: software state file validation failed.\n"));
+        result = TSS2_APP_RC_BAD_TRUST_STATE;
         goto end;
     }
-#endif
-    while(1)
-    {
-        if(key)
-        {
-            grub_free(key);
-            key = NULL;
-            key_len = 0;
-        }
-        grub_memset(passphrase, 0, sizeof(passphrase));
-        grub_printf(N_("Enter hardware passphrase "));
-        if (!grub_password_get (passphrase, 32) || grub_strlen(passphrase) == 0)
-        {
-            grub_cls();
-            grub_printf(N_("Error hardware passphrase not supplied !!!\n"));
-            continue;
-        }
 
-        if(convert_passphrase_to_key(passphrase, grub_strlen(passphrase), &key, &key_len))
-        {
-            grub_cls();
-            grub_printf(N_("Error hardware passphrase wrong !!!\n"));
-            continue;
-        }
-
-        if(validate_hardware_key(key, key_len, buf, buf_len))
-        {
-            grub_cls();
-            grub_printf(N_("Error hardware passphrase wrong !!!\n"));
-            continue;
-        }
-        grub_printf(N_("Passphrase is correct !!!\n"));
-        break;
-    }
-//end:
-    if(key)
+end:
+    if (key)
     {
-        grub_free(key);
+        grub_free (key);
         key = NULL;
-        key_len = 0;
     }
-
     return result;
 }
 
+/*
+ * grub_tpm_integrity_validation - check hardware and software integrity via TPM.
+ *
+ * @halt: if non-zero, prompt for manual passphrase when TPM check fails.
+ */
 grub_err_t
-grub_tpm_integrity_validation(int halt)
+grub_tpm_integrity_validation (int halt)
 {
-    grub_err_t              hard_integrity = 0; //0 means well, others means not well
-    grub_err_t              soft_integrity = 0;
-    grub_err_t              result;
-    grub_uint64_t           endtime;
-    unsigned char           *hard_buf = NULL;
-    unsigned char           *soft_buf = NULL;
-    int                     hard_buf_len = 0;
-    int                     soft_buf_len = 0;
-
-
-    //1.get enc content
-    result = get_hardware_enc_content(&hard_buf, &hard_buf_len);
-    if(result != 0)
+    unsigned char *hard_buf = NULL, *soft_buf = NULL;
+    int            hard_len = 0,     soft_len = 0;
+    grub_err_t     hard_integrity, soft_integrity;
+    grub_err_t     result;
+    grub_uint64_t  endtime;
+
+    result = get_hardware_enc_content (&hard_buf, &hard_len);
+    if (result)
     {
-        grub_printf(N_("Error Bad Hardware State File.\n"));
+        grub_printf (N_("Error: cannot read hardware state file.\n"));
         goto end;
     }
 
-    result = get_software_enc_content(&soft_buf, &soft_buf_len);
-    if(result != 0)
+    result = get_software_enc_content (&soft_buf, &soft_len);
+    if (result)
     {
-        grub_printf(N_("Error Bad Software State File.\n"));
+        grub_printf (N_("Error: cannot read software state file.\n"));
         goto end;
     }
 
-    hard_integrity = tpm_hardware_integrity_validation(hard_buf, hard_buf_len);
-    if(hard_integrity != 0)
+    /* --- Hardware check --- */
+    hard_integrity = tpm_hardware_integrity_validation (hard_buf, hard_len);
+    if (hard_integrity)
     {
-        grub_printf(N_("*********************************************\n"));
-        grub_printf(N_("*    Platform Integrity Has been Broken!    *\n"));
-        grub_printf(N_("*********************************************\n"));
-        if(halt)
-        {
-            file_hardware_integrity_validation(hard_buf, hard_buf_len);
-        }
+        grub_printf (N_("*********************************************\n"));
+        grub_printf (N_("*    Platform Integrity Has been Broken!    *\n"));
+        grub_printf (N_("*********************************************\n"));
+        if (halt)
+            grub_file_integrity_validation (1);
     }
     else
     {
-        grub_printf("---------------------------------------------\n");
-        grub_printf("|       Platform Integrity is Well!         |\n");
-        grub_printf("---------------------------------------------\n");
+        grub_printf (N_("---------------------------------------------\n"));
+        grub_printf (N_("|       Platform Integrity is Well!         |\n"));
+        grub_printf (N_("---------------------------------------------\n"));
     }
 
     endtime = grub_get_time_ms () + 500;
     while (grub_get_time_ms () < endtime);
 
-    soft_integrity = tpm_software_integrity_validation(soft_buf, soft_buf_len);
-    if(soft_integrity != 0)
+    /* --- Software check --- */
+    soft_integrity = tpm_software_integrity_validation (soft_buf, soft_len);
+    if (soft_integrity)
     {
-        grub_printf("*********************************************\n");
-        grub_printf("*  Host OS Kernel&Initrd Has been Broken!   *\n");
-        grub_printf("*********************************************\n");
-        if(halt)
-        {
-            file_software_integrity_validation(soft_buf, soft_buf_len);
-        }
+        grub_printf (N_("*********************************************\n"));
+        grub_printf (N_("*  Host OS Kernel&Initrd Has been Broken!   *\n"));
+        grub_printf (N_("*********************************************\n"));
+        if (halt)
+            grub_file_integrity_validation (1);
     }
     else
     {
-        grub_printf("---------------------------------------------\n");
-        grub_printf("|     Host OS Kernel&Initrd are Well!       |\n");
-        grub_printf("---------------------------------------------\n");
-    }
-    endtime = grub_get_time_ms () + 500;
-    while (grub_get_time_ms () < endtime);
-end:
-    if(hard_buf)
-    {
-        grub_free(hard_buf);
-        hard_buf = NULL;
-        hard_buf_len = 0;
-    }
-    if(soft_buf)
-    {
-        grub_free(soft_buf);
-        soft_buf = NULL;
-        soft_buf_len = 0;
-    }
-    return 0;
-}
-
-grub_err_t
-grub_file_integrity_validation(int halt)
-{
-    grub_uint64_t   endtime;
-    unsigned char           *hard_buf = NULL;
-    unsigned char           *soft_buf = NULL;
-    int                     hard_buf_len = 0;
-    int                     soft_buf_len = 0;
-    grub_err_t              result;
-
-    (void)halt;
-
-    //1.get enc content
-    result = get_hardware_enc_content(&hard_buf, &hard_buf_len);
-    if(result != 0)
-    {
-        grub_printf(N_("Error Bad Hardware State File.\n"));
-        goto end;
+        grub_printf (N_("---------------------------------------------\n"));
+        grub_printf (N_("|     Host OS Kernel&Initrd are Well!       |\n"));
+        grub_printf (N_("---------------------------------------------\n"));
     }
 
-    result = get_software_enc_content(&soft_buf, &soft_buf_len);
-    if(result != 0)
-    {
-        grub_printf(N_("Error Bad Software State File.\n"));
-        goto end;
-    }
-
-    grub_printf("*********************************************\n");
-    grub_printf("*    Platform Integrity Has been Broken!    *\n");
-    grub_printf("*********************************************\n");
-    file_hardware_integrity_validation(hard_buf, hard_buf_len);
-    //grub_cls();
-
-    grub_printf("*********************************************\n");
-    grub_printf("*  Host OS Kernel&Initrd Has been Broken!   *\n");
-    grub_printf("*********************************************\n");
-    file_software_integrity_validation(soft_buf, soft_buf_len);
-    //grub_cls();
-
     endtime = grub_get_time_ms () + 500;
     while (grub_get_time_ms () < endtime);
 
 end:
-    if(hard_buf)
-    {
-        grub_free(hard_buf);
-        hard_buf = NULL;
-        hard_buf_len = 0;
-    }
-    if(soft_buf)
-    {
-        grub_free(soft_buf);
-        soft_buf = NULL;
-        soft_buf_len = 0;
-    }
-    return 0;
+    if (hard_buf) grub_free (hard_buf);
+    if (soft_buf) grub_free (soft_buf);
+    return GRUB_ERR_NONE;
 }
-/*-------------------------------------------------------*/
-#ifndef PATH_MAX
-#define PATH_MAX 4096
-#endif
 
-int part_count = 0;
-typedef struct part_list {
-    char part_name[40];
-    char uuid[38];
-} part_list_t;
+/* -------------------------------------------------------------------------
+ * Passphrase-based fallback validation
+ * ---------------------------------------------------------------------- */
 
-typedef struct part_path {
-    char part_name[40];
-    char path_name[20];
-} part_path_t;
-
-part_list_t parts_info[60];
-part_path_t part_path[20];
-
-static grub_err_t
-grub_get_device_info (const char *name)
+/*
+ * Prompt in an infinite loop until the user enters the correct passphrase
+ * for the hardware state file.
+ */
+static void
+file_hardware_integrity_validation (unsigned char *buf, int buf_len)
 {
-    grub_device_t dev = NULL;
-    char *p;
-    int pi = part_count++;
-    grub_fs_t fs;
-    char *fsname = NULL;
-    char *label = NULL;
-    char *uuid = NULL;
-
-    p = grub_strchr(name, ',');
-    if (p) 
-    {
-        grub_memset(parts_info[pi].part_name, 0, 40);
-        grub_memcpy(parts_info[pi].part_name, name, grub_strlen(name));
-    }   
-
-    p = NULL;
-    p = grub_strchr(name, '/');
-    if (p)
-    {
-        grub_memset(parts_info[pi].part_name, 0, 40);
-        grub_memcpy(parts_info[pi].part_name, name, grub_strlen(name));
-    }
+    char           passphrase[32];
+    unsigned char *key     = NULL;
+    int            key_len = 0;
 
-    dev = grub_device_open (name);
-    if (!dev)
-    {
-        goto end;
-    }
-  
-    if (!dev->disk)
-    {
-        goto end;
-    }
-
-    fs = grub_fs_probe(dev);
-    grub_errno = 0;// ignore all errors
-    if (!fs)
+    while (1)
     {
-        goto end;
-    }
+        if (key) { grub_free (key); key = NULL; key_len = 0; }
+        grub_memset (passphrase, 0, sizeof (passphrase));
 
-    fsname = fs->name;
-    if (grub_strcmp(fsname, "ext2") == 0) 
-    {
-        fsname = "ext*";
-    }
+        grub_printf (N_("Enter hardware passphrase: "));
+        if (!grub_tlcp_password_get (passphrase, sizeof (passphrase)) ||
+            grub_strlen (passphrase) == 0)
+        {
+            grub_tlcp_cls ();
+            grub_printf (N_("Error: hardware passphrase not supplied!\n"));
+            continue;
+        }
 
-    if (fs->fs_label)
-    {
-        (fs->fs_label) (dev, &label);
-        if (grub_errno == GRUB_ERR_NONE)
+        if (convert_passphrase_to_key (passphrase, grub_strlen (passphrase),
+                                       &key, &key_len) != 0)
         {
-            grub_free(label);
+            grub_tlcp_cls ();
+            grub_printf (N_("Error: invalid hardware passphrase format!\n"));
+            continue;
         }
-        grub_errno = GRUB_ERR_NONE;
-    }
 
-    if (fs->fs_uuid)
-    {
-        (fs->fs_uuid) (dev, &uuid);
-        if (grub_errno == GRUB_ERR_NONE)
+        if (validate_key_against_state (key, key_len, buf, buf_len,
+                                        HARDINFO) != 0)
         {
-            if (uuid && grub_strlen(uuid)) 
-            {
-                grub_memset(parts_info[pi].uuid, 0, 38);
-                grub_memcpy(parts_info[pi].uuid, uuid, grub_strlen(uuid));
-            }
-            grub_free (uuid);
+            grub_tlcp_cls ();
+            grub_printf (N_("Error: hardware passphrase incorrect!\n"));
+            continue;
         }
-        grub_errno = GRUB_ERR_NONE;
-    }
 
-end:
-    if (dev)
-    {
-        grub_device_close(dev);
-        dev = NULL;
+        grub_printf (N_("Hardware passphrase accepted.\n"));
+        break;
     }
-    return grub_errno; //added by wbx 06.22 11:57
+    if (key) grub_free (key);
 }
 
-static int 
-grub_get_devices_list (const char *name, void *data)
+/*
+ * Prompt in an infinite loop until the user enters the correct passphrase
+ * for the software state file.
+ */
+static void
+file_software_integrity_validation (unsigned char *buf, int buf_len)
 {
-    int *long_list = data;
+    char           passphrase[32];
+    unsigned char *key     = NULL;
+    int            key_len = 0;
 
-    if (*long_list)
+    while (1)
     {
-        grub_get_device_info (name);
-    }
-
-    return 0;
-}
-
-static grub_err_t
-grub_get_devices (void)
-{
-   int long_list = 1;
-   grub_device_iterate ( grub_get_devices_list, &long_list);
+        if (key) { grub_free (key); key = NULL; key_len = 0; }
+        grub_memset (passphrase, 0, sizeof (passphrase));
 
-   return 0;
-}
-
-int grub_get_namewpart(char *name)
-{
-    int count = 0;
-    char *tmp_name = NULL;
-    char tmp_path[PATH_MAX] = {0};
+        grub_printf (N_("Enter software passphrase: "));
+        if (!grub_tlcp_password_get (passphrase, sizeof (passphrase)) ||
+            grub_strlen (passphrase) == 0)
+        {
+            grub_tlcp_cls ();
+            grub_printf (N_("Error: software passphrase not supplied!\n"));
+            continue;
+        }
 
-    if (name[0]  == '(')
-    {
-        tmp_name = grub_strchr(name, '/');
-    }
-    else if (name[0] == '/')
-    {
-        tmp_name = name;
-    }
-    else
-    {
-        return -1;
-    }
+        if (convert_passphrase_to_key (passphrase, grub_strlen (passphrase),
+                                       &key, &key_len) != 0)
+        {
+            grub_tlcp_cls ();
+            grub_printf (N_("Error: invalid software passphrase format!\n"));
+            continue;
+        }
 
-    if (part_count >= 1) 
-    {
-    	//grub_printf("part_count %d.\n", part_count);
-        for (count = 0; count <= part_count; count++)
+        if (validate_key_against_state (key, key_len, buf, buf_len,
+                                        SOFTINFO) != 0)
         {
-            char *p = NULL;
-	        char *t = NULL;
-            p = grub_strchr(parts_info[count].part_name, ',');
-	        t = grub_strchr(parts_info[count].part_name, '/');
-            if (!p && !t)
-            {
-                continue;
-            }
-            tmp_path[0] = '(';
-            grub_memcpy(&tmp_path[1], parts_info[count].part_name, grub_strlen(parts_info[count].part_name));
-            tmp_path[grub_strlen(parts_info[count].part_name) + 1] = ')';
-            grub_memcpy(&tmp_path[grub_strlen(parts_info[count].part_name) + 2], tmp_name, grub_strlen(tmp_name));
-            grub_file_t file;
-            file = grub_file_open(tmp_path, GRUB_FILE_TYPE_CAT);
-            if (!file)
-            {
-                if (grub_errno != GRUB_ERR_NONE)
-                {
-                    grub_errno = GRUB_ERR_NONE;
-                }
-                //grub_printf("%s not found.\n", tmp_path);
-                grub_memset(tmp_path, 0, 256);
-                continue;
-            }
-            else
-            {
-                grub_file_close (file);
-                grub_memset(name, 0, grub_strlen(name));
-                grub_memcpy(name, tmp_path, grub_strlen(tmp_path));
-                return 1;
-            }
+            grub_tlcp_cls ();
+            grub_printf (N_("Error: software passphrase incorrect!\n"));
+            continue;
         }
-    }
 
-    return -1;
+        grub_printf (N_("Software passphrase accepted.\n"));
+        break;
+    }
+    if (key) grub_free (key);
 }
 
-static void 
-file_measure(char *file_name)
+/*
+ * grub_file_integrity_validation - passphrase-based integrity check.
+ *
+ * Used when: (a) no TPM present, (b) supervisory policy active,
+ * or (c) TPM check failed and halt=1.
+ *
+ * Always prompts for both hardware and software passphrases.
+ */
+grub_err_t
+grub_file_integrity_validation (int halt __attribute__((unused)))
 {
-    char                temp[PATH_MAX] = {0};
-    int                 result;
-    grub_file_t         file = 0;
-    char                *buf = 0;
-    int                 len = 0;    
-    int                 debug_len = 0;
-    int                 i = 0;
-
-    if (!file_name)
-    {
-        return ;
-    }
-
-    grub_memset(temp, 0, PATH_MAX);
-    grub_memcpy(temp, file_name, grub_strlen(file_name));
-
-    result = grub_get_namewpart(temp);
+    unsigned char *hard_buf = NULL, *soft_buf = NULL;
+    int            hard_len = 0,     soft_len = 0;
+    grub_err_t     result;
+    grub_uint64_t  endtime;
 
-    if (result < 0)
-    {
-        return;
-    }
-
-    //grub_printf("%s to be measured.\n", temp);
-
-    file = grub_file_open(temp, GRUB_FILE_TYPE_CAT);
-    if ((!file) || (!file->size))
+    result = get_hardware_enc_content (&hard_buf, &hard_len);
+    if (result)
     {
+        grub_printf (N_("Error: cannot read hardware state file.\n"));
         goto end;
     }
 
-    len = file->size;
-    buf = grub_malloc (len + 1);
-
-    grub_memset(buf, 0, len + 1);
-    debug_len = (len > 16) ? 16 : len;
-
-    if(grub_file_read (file, buf, len) != len)
+    result = get_software_enc_content (&soft_buf, &soft_len);
+    if (result)
     {
+        grub_printf (N_("Error: cannot read software state file.\n"));
         goto end;
     }
-#if 0
-    for(i = 0; i < debug_len; i++)
-    {
-        grub_printf("0x%.2x ", buf[i]);
-    }
-    grub_printf("\n");
-#endif
-    grub_tpm_measure ((unsigned char *)buf, len, GRUB_STRING_PCR, "grub_measured_file");
-
-end:
-    if(buf)
-    {
-	grub_memset(buf, 0, len + 1);
-        grub_free(buf);
-        buf = 0;
-        len = 0;
-    }
 
-    if (file)
-    {
-        grub_file_close(file);
-    }
-    return ;
-}
+    grub_printf (N_("*********************************************\n"));
+    grub_printf (N_("*    Platform Integrity Has been Broken!    *\n"));
+    grub_printf (N_("*********************************************\n"));
+    file_hardware_integrity_validation (hard_buf, hard_len);
 
-/*-------------------------------------------------------*/
+    endtime = grub_get_time_ms () + 500;
+    while (grub_get_time_ms () < endtime);
 
-static void 
-conf_parse(char *buf, int buf_len)
-{
-    char temp[PATH_MAX + 1];
-    int  i = 0, j = 0;
-    int  count = 0;
+    grub_printf (N_("*********************************************\n"));
+    grub_printf (N_("*  Host OS Kernel&Initrd Has been Broken!   *\n"));
+    grub_printf (N_("*********************************************\n"));
+    file_software_integrity_validation (soft_buf, soft_len);
 
-    while (i < buf_len) 
-    {
-        grub_memset(temp, 0, PATH_MAX + 1);
-        while ((i < buf_len - 1) 
-		&& (buf[i] != '/' 
-            || (buf[i] == '/' && i > 0 && buf[i - 1] == '<'))
-        && (buf[i] != '('))
-        {
-            i++;
-        }
-        if (i >= buf_len - 1)
-        {
-            break;
-        }
+    endtime = grub_get_time_ms () + 500;
+    while (grub_get_time_ms () < endtime);
 
-        while (buf[i] != '<')
-        {
-            temp[j] = buf[i];
-            i++;
-            j++;
-            if (j >= PATH_MAX)
-            {
-                while (buf[i] != '<')
-                {
-                    i++;
-                }
-                j = 0;
-            }
-        }
-        if (j != 0)
-        {
-            temp[j] = '\0';
-            //grub_printf("%s to be measured.\n", temp);
-            file_measure(temp);
-            count++;
-        }
-        j = 0;
-    }
+end:
+    if (hard_buf) grub_free (hard_buf);
+    if (soft_buf) grub_free (soft_buf);
+    return GRUB_ERR_NONE;
 }
 
+/* -------------------------------------------------------------------------
+ * PCR measurement of the configuration file
+ * ---------------------------------------------------------------------- */
+
+/*
+ * grub_measure_configured_files - read /boot/host_st_configure.xml and
+ * extend its content into PCR 12.
+ *
+ * This ties the GRUB configuration to the PCR state evaluated during
+ * the software key Unseal.  If the file is absent, the function is a no-op.
+ */
 grub_err_t
-grub_measure_configured_files(void)
+grub_measure_configured_files (void)
 {
-    grub_err_t          result = 0;
-    grub_file_t         file = 0;   
-    char                *buf = 0;
-    int                 len = 0;
+    grub_err_t  result = GRUB_ERR_NONE;
+    grub_file_t file   = 0;
+    char       *buf    = 0;
+    int         len    = 0;
 
-    grub_get_devices();
-
-    file = grub_file_open(MEASUREMENT_CONFIG_FILE, GRUB_FILE_TYPE_CAT);
-    if ((!file) || (!file->size))
+    file = grub_file_open (MEASUREMENT_CONFIG_FILE, GRUB_FILE_TYPE_CAT);
+    if (!file || !file->size)
     {
+        /* Configuration file absent — nothing to measure */
         if (grub_errno != GRUB_ERR_NONE)
-        {
             grub_errno = GRUB_ERR_NONE;
-        }
         goto end;
     }
 
     len = file->size;
     buf = grub_malloc (len + 1);
-
-    if(grub_file_read (file, buf, len) != len)
+    if (!buf)
     {
+        result = GRUB_ERR_OUT_OF_MEMORY;
         goto end;
     }
 
-    grub_tpm_measure ((unsigned char *)buf, len, GRUB_STRING_PCR, "grub_measured_file");
+    if (grub_file_read (file, buf, len) != len)
+        goto end;
 
-    conf_parse(buf, len);
+    /*
+     * Extend the raw file content into PCR 12 (TLCP_APPLICATION_PCR).
+     * grub_tpm_measure() hashes buf with SHA256/SM3 and calls
+     * EFI_TCG2_PROTOCOL.hash_log_extend_event() internally.
+     */
+    grub_tpm_measure ((unsigned char *)buf, (grub_size_t)len,
+                      TLCP_APPLICATION_PCR,
+                      "iTrustMidware:host_st_configure.xml");
 
 end:
-    if(buf)
-    {
-        grub_free(buf);
-	buf = 0;
-	len = 0;
-    }
-
-    if (file)
-    {
-        grub_file_close(file);
-    }
+    if (buf)  grub_free (buf);
+    if (file) grub_file_close (file);
     return result;
 }
diff --git a/grub-core/kern/tlcp_aes.c b/grub-core/kern/tlcp_aes.c
index bdc91de..d38def2 100644
--- a/grub-core/kern/tlcp_aes.c
+++ b/grub-core/kern/tlcp_aes.c
@@ -1,4 +1,7 @@
 #include <grub/tlcp_aes.h>
+#include <grub/misc.h>
+/* Use GRUB's memcpy in freestanding environment */
+#define memcpy grub_memcpy
 
 #define rot1(x) (((x) << 24) | ((x) >> 8))
 #define rot2(x) (((x) << 16) | ((x) >> 16))
diff --git a/include/grub/efi/tlcp.h b/include/grub/efi/tlcp.h
index 550efc9..c357bfb 100644
--- a/include/grub/efi/tlcp.h
+++ b/include/grub/efi/tlcp.h
@@ -1,5 +1,9 @@
-#ifndef GRUB_EFI_TPM_HEADER
-#define GRUB_EFI_TPM_HEADER 1
+/*
+ *  GRUB  --  GRand Unified Bootloader
+ *  iTrustMidware TLCP EFI header (placeholder, declarations are in grub/tlcp.h)
+ */
 
+#ifndef GRUB_EFI_TLCP_HEADER
+#define GRUB_EFI_TLCP_HEADER 1
 
 #endif
diff --git a/include/grub/tlcp.h b/include/grub/tlcp.h
index 93948b5..a1d02e8 100644
--- a/include/grub/tlcp.h
+++ b/include/grub/tlcp.h
@@ -1,10 +1,13 @@
+/*
+ *  GRUB  --  GRand Unified Bootloader
+ *  iTrustMidware TLCP trusted boot interface for grub2-2.12
+ */
+
 #ifndef GRUB_TLCP_HEADER
 #define GRUB_TLCP_HEADER 1
 
-#include <grub/err.h>
-#include <grub/symbol.h>
-
-enum TSS2_APP_RESPONSE_CODE{
+/* Policy state error codes */
+enum TSS2_APP_RESPONSE_CODE {
     TSS2_APP_RC_BASE,
     TSS2_APP_RC_BAD_IO,
     TSS2_APP_RC_BAD_NO_LICENSE,
@@ -30,51 +33,49 @@ enum TSS2_APP_RESPONSE_CODE{
     TSS2_APP_RC_END
 };
 
-
+/* Policy state flags */
 #define POLICY_STATE_TPM_MASK     (grub_err_t)(0x00FF0000)
 #define POLICY_STATE_CHEKC_MASK   (grub_err_t)(0x000000FF)
 #define POLICY_STATE_NONE         (grub_err_t)(0x00000000)
 #define POLICY_STATE_AUDIT        (grub_err_t)(0x00000001)
 #define POLICY_STATE_SUPERVISORY  (grub_err_t)(0x00000002)
-#define POLICY_STATE_TPM_CHECK    (grub_err_t)(0x00010000) //check integrity with tpm
+#define POLICY_STATE_TPM_CHECK    (grub_err_t)(0x00010000)
 
-grub_err_t EXPORT_FUNC(grub_tpm_policy_state)(void);
-grub_err_t EXPORT_FUNC(grub_file_policy_state)(void);
-grub_err_t EXPORT_FUNC(grub_tpm_integrity_validation)(int halt);
-grub_err_t EXPORT_FUNC(grub_file_integrity_validation)(int halt);
-grub_err_t EXPORT_FUNC(grub_measure_configured_files)(void);
+/* TLCP application PCR for configuration file measurement */
+#define TLCP_APPLICATION_PCR      12
+
+/*
+ * Public API called from boot.c
+ * These functions are compiled into the GRUB kernel (not a module).
+ */
+grub_err_t grub_tpm_policy_state (void);
+grub_err_t grub_file_policy_state (void);
+grub_err_t grub_tpm_integrity_validation (int halt);
+grub_err_t grub_file_integrity_validation (int halt);
+grub_err_t grub_measure_configured_files (void);
+
+/*
+ * EFI-specific helpers (implemented in kern/efi/tlcp.c).
+ * Available only on EFI platforms.
+ */
 #if defined (GRUB_MACHINE_EFI)
-grub_err_t check_tpm_policy_state(void);
-grub_err_t check_file_policy_state(void);
-grub_err_t get_software_enc_content(unsigned char **out, int *out_len);
-grub_err_t get_hardware_enc_content(unsigned char **out, int *out_len);
-grub_err_t get_software_dec_key(unsigned char **out, int *out_len);
-grub_err_t get_hardware_dec_key(unsigned char **out, int *out_len);
+grub_err_t check_tpm_policy_state (void);
+grub_err_t check_file_policy_state (void);
+grub_err_t get_software_enc_content (unsigned char **out, int *out_len);
+grub_err_t get_hardware_enc_content (unsigned char **out, int *out_len);
+grub_err_t get_software_dec_key (unsigned char **out, int *out_len);
+grub_err_t get_hardware_dec_key (unsigned char **out, int *out_len);
 #else
-static inline grub_err_t check_tpm_policy_state(void)
-{
-        return -1;
-};
-static inline grub_err_t check_file_policy_state(void)
-{
-        return -1;
-};
-static inline grub_err_t get_software_enc_content(unsigned char **out, int *out_len)
-{
-        return -1;
-};
-static inline grub_err_t get_hardware_enc_content(unsigned char **out, int *out_len)
-{
-        return -1;
-};
-static inline grub_err_t get_software_dec_key(unsigned char **out, int *out_len)
-{
-        return -1;
-};
-static inline grub_err_t get_hardware_dec_key(unsigned char **out, int *out_len)
-{
-        return -1;
-};
+static inline grub_err_t check_tpm_policy_state (void) { return -1; }
+static inline grub_err_t check_file_policy_state (void) { return -1; }
+static inline grub_err_t get_software_enc_content (unsigned char **out __attribute__((unused)),
+                                                    int *out_len __attribute__((unused))) { return -1; }
+static inline grub_err_t get_hardware_enc_content (unsigned char **out __attribute__((unused)),
+                                                    int *out_len __attribute__((unused))) { return -1; }
+static inline grub_err_t get_software_dec_key (unsigned char **out __attribute__((unused)),
+                                                int *out_len __attribute__((unused))) { return -1; }
+static inline grub_err_t get_hardware_dec_key (unsigned char **out __attribute__((unused)),
+                                                int *out_len __attribute__((unused))) { return -1; }
 #endif
 
-#endif
+#endif /* GRUB_TLCP_HEADER */
